# Замеры

Всё получено на выпущенных чекпойнтах, без обучения. Каждый пункт — что
меряли, что вышло, и чего из этого НЕ следует.

---

## 1. Декодер ActionCodec принимает сумму, а не набор кодов ⭐

**Дата:** 13 авг 2026. **Как:** чтение кода, `third_party/actioncodec`.

```python
# actioncodec/modeling_actioncodec.py:271
z_q = self.vq.from_codes(indices)[0]      # [0] — это СУММА
# actioncodec/modeling_actioncodec.py:303
x_recon, padding_mask = self.decoder(z_q, embodiment_ids, durations)
```

`ResidualVectorQuantize.from_codes` (`actioncodec/rvq.py:403`) возвращает пару:

```python
z_q = 0.0
for i in range(n_codebooks):
    z_p_i = self.quantizers[i].decode_code(codes[..., i])
    z_p.append(z_p_i)
    z_q = z_q + self.quantizers[i].out_project(z_p_i)
return z_q, torch.cat(z_p, dim=-1), codes
```

В декодер уходит `z_q` — сумма. Конкатенация по уровням `z_p` не используется.

**Следствие, важное для плана.** Декодер не может знать, каким кортежем кодов
порождена сумма. Поэтому «нарушение остаточной семантики» на уровне декодера
не существует — нарушать нечего.

Проблема при этом **не исчезает, а переформулируется**: вопрос не «согласован
ли кортеж», а **не ушла ли сумма из области, на которой декодер обучался**.
При подмене кода грубого уровня со старым суффиксом сумма сдвигается на
`Δe = e₁(v) − e₁(c₁)`, и этот сдвиг может вывести её за пределы облака,
порождаемого кодировщиком.

А переквантизация суффикса из §4.1 плана — это в точности **проекция
предсказанной чистой латенты на достижимое множество сумм**, жадно по уровням.
Операция в пространстве латент; иерархия входит лишь как способ проекции.

**Что это значит для новизны.** Заявка «чиним остаточную семантику» не
проходит. Живая версия — измерительная и применимая ко всему семейству:

> итеративная и параллельная генерация над RVQ порождает кумулятивные латенты
> вне области кодировщика, и декодеры там деградируют

Это относится и к ResGen, и к ActionCodec-BAR, и к любому параллельному
генератору над RVQ, то есть находка про класс методов, а не про наш.

Про раскладку плоских токенов см. §2: докстринг `decode()` утверждает
перемежение по времени, но код делает поуровневую свёртку. Верить коду.

---

## 2. Устаревший суффикс стоит дороже, чем объясняет смещение суммы ⭐

**Дата:** 14 авг 2026, исправленная версия. **Как:**
`experiments/k1_residual_cost.py`, ActionCodec (2048 кодов, 3 уровня, 16
позиций, чанк 20x7), 96 чанков LIBERO-10.

> ⚠️ **Первая версия этого замера дала противоположный вывод и была неверна.**
> Разбор ошибок — в §2а ниже. Верить настоящему разделу.

Подменяем код в ОДНОЙ позиции на уровне `l`. Вариант **A** — суффикс остаётся
старым, **B** — суффикс переквантован жадно, только в этой позиции.
Сравнение при равной ошибке латенты, по корзинам, кластерный бутстрап по
чанкам. Режим `local` (кандидат из 16 ближайших по словарю) соответствует
поведению генератора; `uniform` — контраст.

| уровень | режим | A/B | 95% ДИ |
|---|---|---|---|
| 0 | **local** | **1.39** | **[1.29, 1.49]** |
| 0 | uniform | 1.22 | [1.17, 1.28] |
| 1 | local | 1.12 | [1.07, 1.16] |
| 1 | uniform | 1.10 | [1.04, 1.12] |
| 2 | — | 1.00 | тождественно, суффикса нет |

Отношения по корзинам на уровне 0 растут монотонно: 1.06, 1.25, 1.22, 1.33,
1.47, 1.47, 1.44, 1.55. Перекос выравнивания везде |·| ≤ 0.08. Пол кодека
0.0066 размаха, ошибки в корзинах от 0.009 до 0.035 — верхние корзины
уверенно над полом.

**Вывод.** При равном смещении суммы устаревший суффикс декодируется хуже, и
на самом грубом уровне заметно — примерно в 1.4 раза. Эффект слабеет с
глубиной, что согласуется с концентрацией влияния на первых уровнях (§3).

**Конфаунд «величина против направления» разведён** — см. §2в. Коротко:
эффект уцелел при ТОЧНОМ уравнивании нормы (1.27), и наивные 1.43
раскладываются на ≈1.27 от направления и ≈1.13 от уменьшения нормы.

**Что остаётся твёрдым.** Декодер получает только сумму (§1), поэтому
объяснение обязано быть геометрическим — про то, куда уехала сумма, а не про
«несогласованность кортежа». Слова `tuple consistency`, `residual repair`,
`hierarchy restoration` неприменимы.

---

## 2а. Разбор ошибок первой версии K-1

Первая версия дала медиану A/B = 1.05 и вывод «явления нет». Вывод был
ошибочным из-за трёх дефектов постановки; после исправления вышло 1.39.

1. **Уровни сваливались в общие корзины.** У последнего уровня суффикса нет,
   A ≡ B, отношение ровно 1.00 — и эта **треть всех замеров** механически
   тянула медиану внутри каждой корзины к единице. Разбирать надо по уровням,
   последний держать отдельным юнит-тестом.
2. **Переквантизация шла по всем 16 позициям**, хотя подмена делалась в одной.
   Причём в нетронутых позициях суффикс тоже менялся: кодировщик выбирал код
   против остатка НЕпроквантованной латенты, а мы квантовали против суммы уже
   проквантованных векторов — цели разные. То есть сравнивались два разных
   вмешательства. Лечится `assert`, что чужие позиции не изменились.
3. **Подстановки брались равномерно по 2048 кодам.** Генератор предлагает
   правдоподобные коды, рядом с текущим. Равномерный режим занижает эффект:
   1.22 против 1.39 на уровне 0. Тот же класс ошибки, что был в
   `head_geometric` с равномерной целью — **режим замера должен совпадать с
   режимом работы**.

Дополнительно отсутствовали доверительные интервалы (замеры сильно
коррелированы внутри чанка — нужен кластерный бутстрап по чанкам), и жадная
переквантизация ошибочно называлась «наилучшим приближением» — она жадная.

**Урок.** Отрицательный результат требует такой же придирчивости, как
положительный. Проверялась ловушка «артефакт выглядит как эффект», и не
проверялась обратная — «дефект постановки прячет эффект».

---

## 2б. Устаревшая запись (для истории)

Первая версия K-1 давала медиану 1.05 и вывод «отдельного явления нет».
Разбор — в §2а. Числа не воспроизводить и не цитировать.

---

---

## 2в. Механизм: декодер чувствителен к смещениям ВНУТРИ кодового подпространства ⭐

**Дата:** 14 авг 2026. **Как:** `experiments/k1c_direction_control.py`,
96 чанков. Возможно потому, что `_decode` (`modeling_actioncodec.py:279`)
принимает латенту напрямую — можно подавать произвольные смещения, не только
узлы кодовой решётки. Сверка пути через латенту с путём через токены: 0.00e+00.

### Часть 1: эффект уцелел при ТОЧНОМ уравнивании нормы

Настоящие смещения A и B, отмасштабированные к тождественно равной норме:

| норма | A | B | A/B |
|---|---|---|---|
| 0.004 | 0.0067 | 0.0056 | 1.20 |
| 0.008 | 0.0134 | 0.0107 | 1.26 |
| 0.015 | 0.0286 | 0.0222 | 1.29 |
| 0.030 | 0.0585 | 0.0448 | 1.31 |
| 0.060 | 0.1128 | 0.0890 | 1.27 |

Устойчиво на пятнадцатикратном диапазоне. Значит **1.39 из §2 раскладывается**:
≈1.27 даёт направление, остаток ≈1.1 был неполным выравниванием величины
внутри корзин. Основная часть эффекта настоящая.

### Часть 2: анизотропии МЕЖДУ УРОВНЯМИ нет

| норма | ур.0 | ур.1 | ур.2 | изотропное |
|---|---|---|---|---|
| 0.004 | 0.0037 | 0.0035 | 0.0034 | 0.0028 |
| 0.008 | 0.0080 | 0.0075 | 0.0071 | 0.0056 |
| 0.015 | 0.0152 | 0.0142 | 0.0141 | 0.0104 |
| 0.030 | 0.0338 | 0.0309 | 0.0288 | 0.0226 |
| 0.060 | 0.0543 | 0.0557 | 0.0565 | 0.0419 |

Отношение ур.0/ур.2 составляет 1.08–1.17 и **переворачивается** на максимальной
норме (0.96). Гипотеза «декодер чувствительнее к грубым осям» **опровергнута**.

**Зато крупная анизотропия есть между словарём и случайным направлением:**
ур.0/изотропное = 1.32, 1.43, 1.46, 1.50, 1.30. Смещения вдоль словаря вредят
в полтора раза сильнее случайных той же длины — разности векторов словаря
лежат в подпространстве, где живёт реальная вариация действий, а случайное
направление в 512 измерениях почти ортогонально ему.

### Механизм НЕ УСТАНОВЛЕН

Проверены и **опровергнуты** три объяснения подряд.

**1. «Ломается остаточная семантика».** Невозможно: декодер видит только сумму
(§1) и о составе кортежа ничего не знает.

**2. «Декодер чувствительнее к грубым осям».** Опровергнуто прямо: отношение
чувствительностей ур.0/ур.2 составляет 1.08–1.17 и переворачивается на большой
норме (0.96).

**3. «δ_B уходит в менее чувствительные направления».** Опровергнуто дважды.
Распределения энергии по полосам спектра почти совпадают, и в самой
чувствительной полосе у δ_B их даже больше (0.162 против 0.145).

**Решающая проверка — `k1e_quadratic_model.py`.** Отклик оказался однородным
(при удвоении нормы множители 2.07, 2.13, 2.03), значит квадратичная модель
`отклик² ≈ Σ_k s_k²·c_k²` применима, и отношение обязано вычисляться из
координат смещений. Чувствительность по узким полосам, на единицу энергии:

| ГК | 1 | 2 | 3–4 | 5–8 | 9–16 | 17–32 | **33–64** | **65–128** | 129–256 | 257–512 |
|---|---|---|---|---|---|---|---|---|---|---|
| | 0.72 | 0.93 | 0.88 | 1.08 | 0.95 | 0.89 | **1.94** | **1.43** | 0.41 | 0.10 |

Анизотропия почти двадцатикратная, пик на ГК 33–64 — там, где у словаря около
процента энергии. Но предсказание не сошлось:

| | A | B | A/B |
|---|---|---|---|
| предсказано из координат | 0.01678 | 0.01715 | **0.979** |
| измерено | 0.02755 | 0.02170 | **1.269** |

Предсказание даёт слегка ОБРАТНЫЙ знак. Абсолютные величины тоже занижены: B
в 1.27 раза, A в 1.64.

**Значит отклик зависит от точки `h0`, а не только от направления смещения.**
Фиксированной анизотропной метрики в латентном пространстве недостаточно.

**Ведущая гипотеза, НЕ проверенная.** δ_B строится С ИСПОЛЬЗОВАНИЕМ `h0` — это
остаток после подгонки под целевую латенту в данной точке, — тогда как δ_A
от `h0` не зависит вовсе. Поэтому δ_B может систематически ложиться вдоль
направлений, которые локальная структура кода в этой точке «поглощает». Это
свойство локальное, и глобальная PCA его уловить не может. Проверять надо
якобианом декодера в точке; работа существенно тяжелее, отдача неясна.

### Что записывать в статью

**Факт:** при точно равной норме смещения устаревший суффикс декодируется в
1.27 раза хуже; практическое правило — проецировать смещение, выигрыш больше,
чем от одного лишь укорочения.

**Механизм:** не установлен. Три объяснения опровергнуты измерением, включая
самое естественное. Так и писать — это честнее и информативнее, чем
правдоподобная догадка.

**Побочный результат, самостоятельный:** декодер сильно анизотропен
(двадцатикратно), и пик чувствительности приходится на направления, где
словарь почти не тратит энергию. То есть кодек размещает ёмкость НЕ там, где
декодер чувствителен. Это утверждение о конструкции кодека, проверяемое на
любом кодеке с непрерывной латентой.

---

## 3. Концентрация эффекта на первом токене

**Как:** подстановка всех кодов в одну позицию, замер сдвига декодированного
чанка (макс-норма по чанку, в долях размаха действий).

| | 1-я позиция | 2-я | 3-я | 4-я | остальные |
|---|---|---|---|---|---|
| VQ-VLA (RVQ, 4 уровня, словарь 256) | **53%** | 21% | 16% | 10% | — |
| OAT (FSQ, 8 регистров, словарь 1000) | **51%** | ~7% каждая | | | |

Межархитектурная инварианта: остаточное квантование против порядкового,
обученный словарь против регулярной решётки, 4 позиции против 8 — доля первой
та же. У OAT разброс между позициями семикратный.

**Зачем:** обоснование неравномерного распределения вычислений по уровням. И
довод в пользу потока против авторегрессии: АР фиксирует доминирующий токен
необратимо.

**Чего не следует:** что перераспределение бюджета по позициям что-то даст.
Это гипотеза, а не вывод.

**Не проверено:** переносится ли на ActionCodec-RVQ.

---

## 4. Геометрия кодов против пространства действий (FSQ)

**Как:** `experiments/c3_grid_vs_action.py`, OAT, 48 чанков, все 1000 кодов на
каждый из 8 регистров.

| величина | значение |
|---|---|
| соседи по решётке ближе по действию | **3.85×** медиана, **25.9×** на 1-м регистре |
| ложных соседей (близко по решётке, далеко по действию) | **0%** |
| ранговая связь глобально | 0.726 |
| она же внутри окрестности из 20 | 0.414 |

**Читать так:** на регулярной решётке FSQ близость кодов уже является
физической метрикой; считать её через декодер незачем. Крупный масштаб
передаётся хорошо, тонкий порядок внутри окрестности — плохо.

**Не проверено на RVQ.** Словарь обученный и нерегулярный, плотность кодов
неравномерна. Ответ может быть противоположным, и это kill-тест §7.1 плана.

---

## 5. Пол разрешения и локальность политики

| | OAT (FSQ) | VQ-VLA (RVQ) |
|---|---|---|
| минимальный достижимый шаг | 1.0% размаха | 3.4% размаха |
| p(жадного кода) у политики | 0.22–0.31 | 0.50–0.70 |
| доля пути от идеальной локальности к случайной | 13–24% | 35–44% |

Регулярная решётка даёт втрое более тонкий пол и втрое более локальную
политику. Политика OAT выучила геометрию решётки сама (ранговая связь
вероятностей с расстоянием 0.35–0.73), хотя софтмакс плоский, а индексация
смешанным основанием.

---

## 6. Лестница по префиксу (OAT)

Ошибка реконструкции при декодировании из `k` первых регистров, в долях
размаха: k=1 → 1.61%, k=2 → 1.23%, k=4 → 1.04%, k=8 → 0.88%.

Чистые ступени только на степенях двойки; **k=5 хуже k=4** — следствие
`token_dropout_mode: 'pow2'` в обучении OAT. Пример того, как деталь
обучения токенизатора проступает в его поведении.

---

## 7. Геометричная голова политики

**Как:** `experiments/head_geometric.py`, синтетика, без данных.

Шаг градиента понижает вероятность кода, выбранного самой головой. Меряется,
насколько дальше уходит освободившаяся масса по сравнению с пропорциональным
перераспределением, при уравненной уверенности (p(моды) = 0.25).

| голова | дальность | параметров |
|---|---|---|
| плоская (Linear→K) | **1.01** | 257 257 |
| факторизованная по координатам | 1.24 | 5 912 |
| смесь в латенте M=4 | **1.99** | 9 253 |
| смесь в латенте M=8 | **2.23** | 18 505 |

Плоская даёт ~1.0 как и требует теория — проверка реализации.

Развёртка по ширине компоненты при удержанной уверенности монотонна:
σ=0.2 шага решётки → 1.17, σ=1.0 → 1.47, σ=2.4 → 2.42. То есть σ — настоящая
ручка радиуса обобщения, и её надо калибровать, а не брать с потолка.

**Где пригодится здесь:** §12 плана отмечает, что жадная переквантизация
против усреднённой `ĥ` смажет моды. Смесевая голова в латенте — ответ на это,
и методика калибровки σ готова. Для RVQ шаг нерегулярен, калибровать заново.

---

## 7б. Разреженность по токенам действия не даёт wall-clock ⭐

`experiments/k4a3_latency_probe.py`, чекпойнт BAR, Tesla V100-SXM2-32GB,
bfloat16, прогрев 50, замеров 200, синхронизация вокруг каждого прогона.
Число активных позиций задаётся параметром `bos_len` их же метода
`_run_action_sequence` при неизменном префиксе VLM.

Главное число — **прямое отношение измеренных медиан**, без подгонки:

| batch | t(16)/t(4) | t(16)/t(1) | префикс VLM | сквозной вызов |
|---|---|---|---|---|
| 1 | **1.0333** | 1.0594 | 69.9 мс | **273.3 мс** |
| 4 | 1.0154 | 1.0279 | 240.9 мс | 914.8 мс |

Подгонка **только по рабочему диапазону q ≤ 16** (точки 32 и 48 в него не
входят и утягивали наклон вдвое):

| batch | t(q) = a + b·q, мс | R² | макс. остаток | доля позиций при q=16 |
|---|---|---|---|---|
| 1 | 62.39 + 0.2149·q | 0.924 | 0.59 мс | **5.2%** |
| 4 | 211.20 + 0.3514·q | 0.930 | 0.92 мс | 2.6% |

Полная генерация трёх блоков — 198.9 мс при batch 1, то есть 3 × 66 мс:
**кэша KV нет, префикс из 168 токенов пересчитывается на каждом блоке**.
Сквозной вызов измерен целиком (складывать медианы разных распределений
некорректно): 273.3 мс против 268.8 у суммы медиан, расхождение 1.7%.

**ПОПРАВКА K-5a: свободный член 62.39 мс — НЕ префикс VLM целиком.** Прямое
измерение с кэшем префикса (`k5a_prefix_cost_probe.py`, fp16, пять серий с
перемешанным порядком) разложило блочный проход пополам: **башня VLM 23.6 мс,
экспертная башня 25.8 мс**. Подгонка загнала в свободный член фиксированную
стоимость экспертной башни — она не зависит от числа позиций, но и от кэша не
выигрывает. Поэтому оценка «убрать два лишних пересчёта = 1.84×» ЗАВЫШЕНА;
измеренное ускорение блочной части — **1.383×** [1.326, 1.543], около 1.45× без
остаточных накладных прототипа. Абсолютные времена §7б и K-5a несравнимы: там
модель фактически грузилась во float32 (`from_pretrained` игнорирует запрошенный
dtype), здесь приведена к float16 принудительно. Структурная поправка от dtype
не зависит: башни соизмеримы по стоимости.

**РАЗЛОЖЕНИЕ ПОЛНОГО ВЫЗОВА (K-5a2, `k5a_pipeline_bench.py`, fp16, batch 1,
пять серий с перемешанным порядком, длина префикса ПО ФАКТУ 171 токен):**

| часть | мс | доля |
|---|---|---|
| процессор (CPU: кроп, шаблон, токенизация) | 15.59 | 7.9% |
| префикс VLM (зрение + склейка эмбеддингов) | 22.34 | 11.3% |
| блоки 1–3 | **147.01** | **74.3%** |
| декод ActionCodec | 9.95 | 5.0% |
| **весь вызов** | **197.84** | 100% |

Целое измерено отдельно, а не сложением медиан; расхождение с суммой частей
1.5%. Разброс целого по сериям 10.4 мс, то есть 5% — заметно устойчивее
блочных замеров.

Внутри блочной части, по разложению K-5a: **~71 мс башня VLM** (трижды
пересчитанная) и **~77 мс экспертная башня**.

**ПОПРАВКА К ПРЕЖНЕМУ УТВЕРЖДЕНИЮ О ДОЛЕ ACTION-ЧАСТИ.** Ранее из наклона
подгонки `0.2149 мс на токен` делался вывод, что вычисления на токенах действия
стоят ~3.4 мс на проход, доля action-части после кэша `f ≈ 0.07`, и потому
адаптивное число проходов даёт около 1.02×. **Это неверно примерно в семь раз.**
Наклон описывает лишь ПРЕДЕЛЬНУЮ добавку на токен; фиксированная стоимость
экспертной башни — 24 слоя при hidden 2048 — в него не входит и составляет
~26 мс независимо от числа токенов (16 или 48). После кэширования префикса
экспертная башня занимает БОЛЬШЕ ПОЛОВИНЫ вызова, и пропуск одного блочного
прохода из трёх даёт около **1.20×**.

Потолки при доле блочной части f = 0.748: ускорение блоков в 1.383× даёт
сквозные 1.261×, в 2× — 1.598×, бесконечное — 3.974×. Измеренный кэш префикса
даёт сквозное **1.25×**.

**Кэш префикса корректен.** Маска `bar.py:902` запрещает запросам VLM видеть
токены действия, поэтому башня VLM не зависит от них: расхождение скрытых
состояний при разных токенах — ровно ноль по всем 24 слоям. Прототип с кэшем
даёт побитово те же логиты на всех трёх блоках.

**Разброс на этой машине.** Опорные времена в двух прогонах одного кода
разошлись на 20–25%, разброс по сериям 13–16 мс при медиане 46–49. Абсолютные
величины для заявлений непригодны; годятся только отношения, посчитанные внутри
серии с перемешанным порядком режимов.

Отношение постоянной части к предельной цене позиции — **290:1** при batch 1
(в первой версии отчёта стояло 595:1 по наклону, снятому со всего диапазона
q ≤ 48; правильный наклон вдвое круче).

Что отсюда следует.

1. **Ворота E недостижимы на этой архитектуре.** Потолок одного прохода
   1.033× против требуемых 1.25× на голове. Сквозь весь вызов ещё меньше: при
   расписании «1 плотный блок + 2 разреженных по K=4» экономится
   2 × 12 × 0.215 ≈ 5.2 мс из 273.3, то есть **1.019×** против требуемых
   1.15×. Разница на порядок, аккуратностью реализации не закрывается.
   Kill-критерий 5 плана выполняется.
2. **Хвост латентности тоже не спасает.** p90/p50 = 1.01. Отдельные всплески
   p99 (78.7 мс при q=2) — редкие выбросы планировщика, с числом активных
   позиций не связаны.
3. **Даже идеальный кэш префикса не меняет вывод.** Он убрал бы два
   пересчёта из трёх (≈ 273 → 148 мс, 1.85×), но эта экономия не имеет
   отношения к активному набору. Разреженность поверх кэша дала бы те же
   5.2 мс, теперь из 148, то есть ~3.5%.
4. **В ЭТОЙ архитектуре экономить выгоднее числом проходов.** Постоянная
   часть 62.4 мс на проход против 0.215 мс на позицию — отношение **290:1**.
   Это указывает на ветку «adaptive solver / early stopping» из §12 плана —
   но как на альтернативу, а не как на замену: для отдельного action-only
   refiner вопрос о token sparsity остаётся открытым (см. ниже).

Причина структурная, а не аппаратная: 168 токенов префикса против 16 позиций
действия. Отношение сохранится на любой карте.

**Оговорка про железо.** V100 (Volta) не имеет аппаратного bf16, поэтому
абсолютные времена завышены в несколько раз против A100/H100. Проверка:
0.81 TFLOP на проход префикса при ~15.7 TFLOPS FP32 даёт ≈52 мс — сходится с
измеренными 62.4 мс, то есть проход упирается в арифметику на CUDA-ядрах, а не
в чтение весов (4.8 ГБ при ~900 ГБ/с — это 5.4 мс). Финальный бенчмарк надо
повторить на целевой карте, но **воротное отношение** — долевое, и переносится.

**Чего замер НЕ закрывает.** Меряется выложенный BAR, где токены действия идут
через весь декодер VLM вместе с пересчитываемым префиксом. Целевая архитектура
плана (C1) — отдельный action-only refiner поверх кэшированного контекста; там
позиции действия составляют почти всю стоимость refiner'а, и **token sparsity
для него остаётся НЕПРОВЕРЕННОЙ**.

Требование к C1, выведенное отсюда. Обозначения, **их легко перепутать**:

- `r` — **доля сэкономленной стоимости** refiner, не его ускорение;
- `S_ref = 1/(1 − r)` — ускорение самого refiner;
- `f` — доля refiner в сквозном времени;
- `R` — сквозное ускорение.

Закон Амдала: `R = 1/(1 − f·r)`, откуда `f ≥ (1 − 1/R)/r`. Через ускорение
refiner это записывается как `f ≥ (1 − 1/R)/(1 − 1/S_ref)` — **та же формула**,
просто в другой параметризации.

При расписании «1 плотный шаг по 16 позициям + (T−1) шагов по K», то есть
`r = 1 − (16 + (T−1)K)/(16T)`, при T = 8:

| K | экономия refiner `r` | эквивалент `S_ref` | для 1.15× | для 1.25× |
|---|---|---|---|---|
| 1 | 82.0% | 5.57× | 15.9% | 24.4% |
| 2 | 76.6% | 4.27× | 17.0% | 26.1% |
| **4** | **65.6%** | **2.91×** | **19.9%** | 30.5% |
| 8 | 43.8% | 1.78× | 29.8% | 45.7% |

Зависимость от длины цикла при K=4: T=4 даёт `r`=56.3% и требует `f`≥23.2%;
T=16 даёт `r`=70.3% и требует `f`≥18.6%. Чем короче цикл, тем жёстче
требование: первый плотный шаг не амортизируется.

**Отсюда следует, что пара порогов «≥1.5× на refiner» и «доля ≥20%» не
достаточна:** при `S_ref = 1.5×` (то есть `r` = 33.3%) и `f` = 20% сквозное
ускорение равно 1.071×, а не 1.15×. В `PLAN.md` эти три числа заменены одним
условием `f · r ≥ 0.130`.

**Ловушка при переносе на BAR.** Формула предполагает, что стоимость refiner
пропорциональна числу активных позиций. У BAR это не так: три блочных прохода
занимают 72.8% сквозного времени (198.9 из 273.3) — доля вроде бы много выше
20%, — но внутри прохода на позиции приходится лишь 5.2%, поэтому экономится не
65.6%, а ~3%. Условие `f ≥ 20%` имеет смысл только там, где стоимость
действительно масштабируется с числом позиций.

Зрение (69.5 мс на управляющий шаг) в эту долю не входит. Раньше здесь стояло
24% — это соответствует требованию **снизить время на 15%**, что строже, чем
ускорение 1.15× (оно даёт −13.0%). Везде используется определение через
ускорение.

**Границу вывода не расширять.** Правильная формулировка: «в интегрированной
BAR-архитектуре выгоднее сокращать число проходов; для action-only refiner
вопрос открыт». Adaptive NFE — сильная альтернативная ветка, но переключаться
на неё целиком до проверки action-only refiner преждевременно.

## 7в. Групповой оракул: разреженность подтверждена, наборы в среднем избыточны ⭐

`experiments/k4a2_phase_a.py`, 96 наблюдений из 48 эпизодов, все 16 позиций
вмешательства, смещение 3, окно 4, RMS по непрерывным каналам, кластерный
бутстрап по эпизодам. Точный перебор — ВСЕ 2516 подмножеств размера ≤ 4 на
всей выборке, без подвыборки.

Воспроизводит K-4a на другой выборке: жадный K=4 → 0.90 против 0.90,
JS → 0.51 против 0.51, окно → 0.70 против 0.69, «только p» → 0.15 против 0.14.

### Доля закрытого разрыва при равном числе позиций

ОРАКУЛЫ подсматривают результат; ПРИЧИННЫЕ пользуются только проходом ДО
правки (`lg_before = blk(c0_old)`).

| K | точный ≤4 (орк) | жадный (орк) | одиночный (орк) | окно (орк) | JS (орк) | случ. средь измен. (орк) |
|---|---|---|---|---|---|---|
| 1 | — | 0.52 | 0.52 | 0.52 | 0.17 | 0.17 |
| 2 | — | 0.75 | 0.69 | 0.62 | 0.34 | 0.35 |
| **4** | **0.93** [0.92,0.94] | **0.91** [0.90,0.92] | 0.79 | 0.69 | 0.53 | 0.65 |
| 8 | — | 0.96 | 0.81 | 0.73 | 0.74 | 0.95 |

| K | top-K энтр. ДО (прич.) | top-K запас ДО (прич.) | окно по энтр. ДО | только p | случайно из 16 (20 сид.) |
|---|---|---|---|---|---|
| 1 | 0.13 | 0.12 | 0.13 | 0.16 | 0.04 |
| 2 | 0.24 | 0.20 | 0.18 | 0.16 | 0.09 |
| **4** | **0.40** [0.37,0.44] | 0.39 | 0.27 | 0.16 | 0.18 |
| 8 | 0.65 | 0.64 | 0.47 | 0.16 | 0.38 |

Причинные baseline'ы пересчитаны после исправления направления времени
(прежние 0.37 и 0.28 пользовались проходом ПОСЛЕ правки и отозваны). Энтропия
прохода ДО правки предсказывает лучше, чем ошибочная «после»: 0.40 против 0.37.

Жадный (с правом отказа) сохраняет **97.4%** точного. Победивший набор: размер
4 в 63% случаев, пустой в 1%, протяжённость 9.0 из 16, непрерывен в 14.8%.
Разброс случайного отбора при K=4 по 20 сидам — ст. откл. 0.034 доли разрыва,
поэтому одна реализация была бы недопустима.

### A0. Заявка о разреженности требует двух знаменателей ⭐

Между `stale` и `z_ref` расходятся в среднем **4.79** позиции из 16 (медиана 5,
квартили 3/6, максимум 15); в 49.7% случаев расходится не больше четырёх.

Это не отсутствие разреженности, а **две разные разреженности**: (1) сам
support разрежен — правка coarse меняет тонкие уровни лишь в ~5 позициях из 16,
остальные одиннадцать пересчитывать вообще незачем; (2) внутри support
полезность неоднородна — случайные четыре изменённые позиции дают 0.65,
оптимальные четыре 0.90. Формулировка: полный пересчёт затрагивает небольшое
подмножество позиций, а основная полезность внутри него сосредоточена в
структурированной группе.

При этом «4 из 16» — по СОДЕРЖАНИЮ 4 из ~5. Подтверждается снизу: случайный
выбор среди изменённых даёт при K=4 уже 0.65, а при K=8 — 0.95, то есть отбор
там перестаёт что-либо значить. Baseline «случайно из 16» (0.15) занижен
разбавлением и в качестве опорного числа непригоден.

Правило: **для экономии вычислений знаменатель 16** (плотный проход всё равно
трогает все позиции), **для утверждения о структуре — число затронутых
позиций**. K-4a их смешивал.

### A2. Средняя избыточность выбранных наборов; субмодулярность НЕ проверена ⭐

Масштаб: одиночный выигрыш RMS 0.00411, квадрат 0.000081.

| набор | Δ на RMS | доля | **Δ на КВАДРАТЕ** | **доля** | доля Δ>0 (кв.) |
|---|---|---|---|---|---|
| одиночный top-4 | +0.00027 | +6% | **−0.000032** | **−39%** | 10.4% |
| жадный 4 | +0.00170 | +41% | **−0.000015** | **−19%** | 39.8% |
| точный ≤4 | +0.00200 | +49% | **−0.000012** | **−15%** | 44.9% |

**Знак переворачивается при смене метрики.** На RMS всё выглядело
супераддитивным. Причина — вогнутость корня: самопроверка на разложимой
модели, где взаимодействий нет по построению (`selftest_metric_artifact`),
даёт на RMS +85.2% одиночного выигрыша, на квадрате — ноль до машинной
точности. Столбец RMS информации о взаимодействии не несёт.

**Что именно измерено.** Среднее Δ на квадрате отрицательно для всех трёх
рассмотренных способов выбора, **но Δ положительна в 10.4–44.9% отдельных
случаев**. Это средняя избыточность выбранных наборов, а не свойство функции.
Заявлять субмодулярность по этим числам нельзя: субмодулярность — условие на
ВСЕ пары приращений `Ω(A;q,r) = G(A∪q) + G(A∪r) − G(A) − G(A∪{q,r}) ≥ 0`, и оно
здесь не проверялось. Прямой тест — K-4a4.

Наблюдения, **согласующиеся** с субмодулярностью, но её не доказывающие:

- предельные выигрыши по шагам жадного отбора убывают: 100 / 44 / 21 / 10 / 5 /
  2 / 1 / 0.5 %;
- жадный сохраняет 97.4% точного (для субмодулярных функций такое типично, но
  высокая доля бывает и без субмодулярности).

**Почему жадный обгоняет одиночный.** Набор из индивидуально лучших позиций
избыточен сильнее (−39%), оптимальный — слабее (−15%). Разница **+24.7%**
одиночного выигрыша: жадный **избегает взаимозаменяемых позиций**.

Правдоподобное, но не проверенное объяснение: временное поле восприятия
декодера размазывает каждую латентную позицию по нескольким шагам действия,
поэтому близкие по влиянию позиции чинят одно и то же. Проверяется ядром
перекрытия в фазе B2.

**История ошибки.** Величины Δ на RMS привели к выводу о синергии; на квадрате
среднее Δ отрицательно. Отзывать гипотезу по загрязнённой метрике не следовало.

**Что это значит для новизны.** Если K-4a4 подтвердит приближённую
субмодулярность, «условный последовательный выбор» сведётся к стандартной
жадной максимизации, и заявка ослабнет. Держится независимо от этого: два
**заданных вручную** штрафа за похожесть
(временная близость, косинус между Δh) избыточность НЕ ловят — лучший λ = 0,
любой штраф ухудшает (0.79 → 0.65). Значит структура избыточности нетривиальна;
уловит ли её обучаемый однопроходный предсказатель — вопрос K-4b.

**Что артефакт затронуть НЕ может.** В разложимой модели одиночный выигрыш
`sqrt(T) − sqrt(T − d_q²)` монотонен по `d_q²`, поэтому top-4 по одиночным
баллам и жадный отбор дают **тождественно один набор** (проверено на той же
синтетике). Значит разница 0.79 против 0.91/0.93 и Jaccard 0.468 требуют
настоящей связи между позициями и остаются в силе независимо от метрики.

**Формулировка.** Измеряется избыточность **группового action-repair в
пространстве декодированных действий**, а не зависимость токенов внутри потока.

В K-4b обязательны четыре сравнения — independent top-K, one-shot set,
one-shot multi-segment, sequential.

**Матрица парных взаимодействий из K-4a отозвана как неинформативная:** она
усреднялась по всем 120 парам, где в большинстве хотя бы одна позиция ничего не
даёт и взаимодействие нулевое по построению.

### A3. Multi-segment не прошёл тест эквивалентности; нужен произвольный набор ⭐

При K = 4, оракул внутри каждого семейства:

| семейство | закрытая доля |
|---|---|
| 1 отрезок длины 4 | 0.70 |
| 2 отрезка 2+2 | 0.84 |
| отрезок 2 + два одиночки | 0.89 |
| **≤ 2 отрезков, суммарно 4** | **0.90** |
| произвольный набор (точный) | 0.93 |

**Тест эквивалентности ПРОВАЛЕН.** `D = G_точный − G_multiseg`: точка 0.0373,
95% ДИ [0.0320, 0.0430], односторонняя верхняя граница **0.0420** при пороге
0.03. Интервал целиком выше порога, случай не пограничный.

По зафиксированному до запуска правилу → **основным остаётся произвольный
set-router**, упрощение до двух отрезков не подтверждено. Прежний вывод в
пользу multi-segment был сделан по округлённым точкам (0.90 против 0.93) и
неверен.

Оценка разности проверена на синтетике с заложенным ответом: 0.00 → −0.0000;
0.03 → 0.0303 (порог верно не проходит); 0.10 → 0.1005.

Один отрезок не годится тем более (0.69): оптимальные наборы разбросаны
(протяжённость 9.0 из 16, непрерывны в 14.8%).

### A4. Эффект не создаётся отдельными типами движения

Жадный при K=4: по позиции вмешательства 0.87–0.92, по скорости 0.89–0.93, по
амплитуде правки 0.89–0.92, по 12 задачам 0.86–0.96. При переключении схвата
0.97 (n=64) против 0.90. Устойчиво.

### A5. Масштаб разрыва и схват

Разрыв stale↔полный пересчёт — 0.00789, собственная ошибка модели относительно
датасета — 0.01859, отношение **0.42**. Разрыв не доминирует, но и не
пренебрежим: полный ремонт снимает величину порядка 40% от собственной ошибки.

Схват: расхождение stale с опорой 0.0005, после ремонта 0.0000, модель против
датасета 0.0156. Возмущение схвата практически не задевает — деградации нет,
но и сюжета тоже.

### Итог фазы A

| ворота | порог | значение | итог |
|---|---|---|---|
| нижняя граница ДИ жадного при K=4 | ≥ 0.80 | 0.90 | ПРОЙДЕНО |
| жадный сохраняет от точного | ≥ 95% | 97.4% | ПРОЙДЕНО |
| верхняя граница отставания multi-segment | ≤ 0.03 | 0.0420 | **НЕ ПРОЙДЕНО** |

Держится: полезность ремонта разрежена, нелокальна и независимым ранжированием
не достаётся (0.79 против 0.91/0.93 при том же числе позиций), причём два
заданных вручную штрафа за похожесть её тоже не берут.

Не держится: синергия (выбранные наборы в среднем избыточны), упрощение до
двух отрезков (тест эквивалентности провален), ускорение по времени (§7б).

**Планка для K-4b:** лучший **причинный** отбор при K=4 — **0.40** (top-K по
энтропии прохода ДО правки), оракул 0.91, точный 0.93. Отыграть надо
0.40 → 0.91. Разрыв огромен, поэтому сначала K-4b0: предсказуемы ли вообще
одиночные выигрыши из причинных признаков.

## 7г. Глобальная субмодулярность опровергнута; слоевая структура не подтверждена ⭐

`experiments/k4a4_submodularity_audit.py`, 1536 вмешательств из 48 эпизодов,
все 16 позиций, полный перебор 2517 наборов размера ≤4 на КАЖДОМ вмешательстве.
Всё на квадрате ошибки. Проверяется прямое условие убывающей отдачи

    Omega(A; q, r) = G(A∪q) + G(A∪r) − G(A) − G(A∪{q,r}) ≥ 0

по всем 12720 тройкам с |A| ≤ 2, и монотонность `M(A,q) = G(A∪q) − G(A)` по
9216 парам с |A| ≤ 3.

> ⚠️ **Слоевой вывод про «сильные позиции перекрываются» ОТОЗВАН окончательно:**
> он провалил независимый контроль на обоих сидах, см. ниже. Подтверждены
> глобальная несубмодулярность, отсутствие гарантии жадного отбора и нарушения
> монотонности.

### Что пришлось исправить в постановке

**Знаменатель.** У неизменённой позиции латента совпадает с опорной побитово,
поэтому `G(A∪q) = G(A)` точно и `Omega = 0` тождественно. Содержательны только
тройки, где ОБЕ позиции в changed support — их 10.07%. Деление на все тройки
занижало долю нарушений в десять раз (4.78% вместо 47.5%).

**Метрика ворот из плана непригодна.** «Масса нарушений» — сумма `max(0,−Ω)` по
12720 тройкам, делённая на один скалярный выигрыш; масштаб произвольный, порог
5% неприменим. Заменена на долю нарушений среди содержательных троек.

**Маска слоя сильных позиций была дефектной.** `top-4` строился по одиночным
выигрышам, а у неизменённой позиции выигрыш ровно ноль; при support меньше
четырёх такие позиции попадали в слой и приносили тождественно нулевые тройки,
занижая долю нарушений (12.25% вместо 20.70%). Маска обязана пересекаться с
маской содержательных. Тот же дефект знаменателя был в разбивке по `|A|` и в
макро-статистике. Починка вскрыла более глубокую проблему: само разбиение по
выигрышу оказалось конфаундом, см. раздел про контроль.

**Объём таблиц оценивался неверно.** 1536 × 2517 × 4 = 15.5 **МБ**, а не ГБ
(ошибка в тысячу раз). Поэтому таблицы `G(S)` сохраняются целиком вместе с
масками, эпизодами, позициями и порогами, а вся статистика пересчитывается
оффлайн через `--from-npz`, без модели и кодека.

**Пол точности мерялся не для той величины.** `Ω` — разность четырёх значений
`G`, её численный пол выше, чем у одиночных выигрышей. Теперь сравниваются
полные таблицы `G` во float32 и float64, и `|Ω|/tau` печатается по
ФАКТИЧЕСКОМУ порогу вмешательства, а не по глобальному приближению.

**Точность проверена, а не предположена.** Кодек грузится во float32 (не в
bf16, как я считал). Пол меряется для САМОЙ `Ω` — разности четырёх значений
`G`, у неё он заведомо выше, чем у одиночных выигрышей:

| величина | сид 0 | сид 1 |
|---|---|---|
| **расхождение float32↔float64 для `Ω`** | **4.2e-09** | **4.9e-09** |
| справочно, для одиночных выигрышей | 2.4e-09 | 2.7e-09 |
| порог `1e-3·g₁` | 8.9e-08 | 9.7e-08 |

Порог в 6–7 раз выше пола.

### Результаты, два сида по 1536 вмешательств

| величина | сид 0 | сид 1 |
|---|---|---|
| нарушений среди содержательных троек | **47.38%** [46.33, 48.37] | **47.04%** [46.19, 47.92] |
| строго (`A` внутри support) | 47.01% | 46.62% |
| то же при пороге `τ/g₁ = 1e-2` | 39.15% | 37.26% |
| **нарушений монотонности** | **29.42%** [28.01, 30.67] | **30.55%** [29.15, 31.90] |
| монотонность, строго | 31.81% | 33.31% |
| локальное парное отношение: медиана | 1.003 | 1.003 |
| доля ниже 1 | 48.5% | 48.3% |

Величина нарушения при поэкземплярной нормировке на свой `g1_i`: медиана
3.09 / 2.57%, 90-й процентиль 39.0 / 36.0%, 99-й 413 / 338%. Максимум по этой
нормировке неустойчив (до 40000%), потому что `g1_i` бывает чуть выше порога при
крупной `Ω`; читать надо процентили, а лучше — нормировку на `G_i*`.

**Ущерб от немонотонности, поэкземплярная нормировка** `|M_i(A,q)| / G_i*`,
вмешательства с вырожденным `G_i*` исключены (15 и 19 из 1536):

| величина | сид 0 | сид 1 |
|---|---|---|
| медиана ущерба / свой `G_i*` | 5.78% | 5.26% |
| 90-й процентиль | 36.78% | 36.39% |
| 99-й процентиль | 148.87% | 149.72% |
| **доля нарушений, съедающих >50% своего `G_i*`** | **6.61%** | **6.48%** |
| **то же, >100%** | **2.10%** | **2.11%** |

Прежние числа (медиана 2.32 / 2.57%, 99-й процентиль 87.2 / 100.9%) считались с
ГЛОБАЛЬНОЙ нормировкой: процентиль объединённой по всем вмешательствам величины
делился на средний по выборке разрыв, что не относится ни к какому конкретному
вмешательству. **Отозваны.**

Отдельно: предсказывая направление этого смещения, я утверждал, что глобальная
нормировка раздувает хвост. Измерение показало **обратное** — она его занижала,
и медиана, и 99-й процентиль выросли примерно вдвое. Синтетика давала другой
знак, потому что в ней ущерб был пропорционален `G*`; в данных зависимость иная.
Направление смещения надо измерять, а не выводить из удобного примера.

По размеру `A` доля не растёт: 47.45 / 47.40 / 47.38% (сид 0) и
47.16 / 47.10 / 47.03% (сид 1). Это свойство парных взаимодействий, а не
накопление с глубиной набора.

По двенадцати задачам LIBERO доля нарушений 40.9–50.4% (сид 0) и 44.2–53.4%
(сид 1). На задачу приходится 2–4 эпизода, поэтому разброс внутри диапазона
ничего не значит; значима узость самого диапазона.

Оба прогона воспроизводят друг друга в пределах процентного пункта.

**Оговорка о протоколе.** `--seed` управлял только выборкой данных; генератор
рангов вмешательства был прибит (`manual_seed(1)`), поэтому расписание рангов у
обоих прогонов **тождественно**. Это допустимый протокол, но называть их двумя
независимыми сидами всех источников случайности неправильно. Исправлено:
генератор рангов привязан к `--seed`; приведённые числа получены до правки.

### Слоевой вывод НЕ ПОДТВЕРЖДЁН — контроль провален на обоих сидах

Разбиение по одиночному выигрышу давало выразительную картину: асимметрия
`mean(Ω)/mean(|Ω|)` равна **+0.78 / +0.81** у сильных пар и **−0.73 / −0.65** у
остальных, то есть «сильные избыточны, слабые комплементарны».

Картина не выдержала независимой проверки. Разбиение по выигрышу использует
величины из числителя `Ω = G(q) + G(r) − G(qr)`: отбирая пары с малыми
`G(q), G(r)`, мы никак не ограничиваем `G(qr)`, и `Ω` смещается вниз
механически. Контрольное разбиение по **норме смещения латенты** `‖Δh‖`, не
участвующей в вычислении `Ω`, даёт **+0.11 / +0.18** (сид 0) и **+0.09 / +0.15**
(сид 1) — обе асимметрии положительны и близки, знаковой структуры нет.

Контроль не слабый: `‖Δh‖` разделяет пары по ВЕЛИЧИНЕ взаимодействия почти так
же хорошо, как выигрыш (|Ω| 10.08% против 3.14% при отношении 3.2, у выигрыша
12.18% против 4.76% при отношении 2.6). Он находит, где взаимодействие крупное,
и именно знак не воспроизводит. Пересечение двух определений top-4 — 55.2 и
51.0%.

**Писать «сильные позиции систематически перекрываются» нельзя.** Величины
+0.8 / −0.7 отражают связь с величиной выигрыша, а не с важностью позиции.

Отдельный урок: синтетический отрицательный контроль артефакта не поймал, потому
что был построен как `G(qr) = w_q + w_r + c_qr`, где `Ω = −c_qr` **по
построению независима** от одиночных выигрышей. Синтетика проверяла не тот
механизм, который есть в данных. Записано в `LESSONS.md`.

### Выводы

**Глобально функция НЕ субмодулярна.** Знак взаимодействия произвольной
содержательной пары близок к случайному (47% нарушений, медиана локального
парного отношения 1.003, 48% значений ниже единицы). Функция близка к
МОДУЛЯРНОЙ с симметричными тяжёлыми хвостами. Устойчиво к десятикратному
ужесточению порога и к смене сида.

Формулировка «приближённая субмодулярность» недоступна.

**Классической теоретической гарантии для жадного отбора НЕТ.** Прежняя оценка
`1 − e^{−gamma}` = 0.256 **отозвана как некорректная**: вычислявшаяся величина —
локальное парное отношение для конкретной тройки, а submodularity ratio
Das & Kempe определяется как минимум по допустимым парам множеств. Сверх того,
теорема требует монотонности, а она нарушается в 29–31% случаев. Расчёт удалён,
величина оставлена как описательная статистика.

Близость жадного оракула к точному (97.4%) остаётся **исключительно
эмпирическим** результатом.

**Монотонность нарушается в 29–31% случаев, и в рабочем режиме чаще.** Когда
префикс целиком лежит внутри changed support — то есть ровно там, где окажется
router, — доля растёт до 31.8–33.3%. Отсюда: у оракула должно быть право отказа
(пустой набор включён в перебор), router нельзя учить правилу «чем больше
позиций, тем лучше», а `L_BCE` по changed-support не может быть основной целью
в B1: изменённая позиция не значит полезная.

### Что это задаёт для B1/B2

Решение о продолжении B0/B1 опирается на **измеренный разрыв** между причинным
baseline (0.40), независимым отбором (0.79), жадным (0.91) и точным (0.93)
оракулами, а не на субмодулярную теорию.

Указание «учить router распознаванию перекрытия сильных позиций» **снято**:
слоевой вывод провалил контроль. Ядро перекрытия из декодера в фазе B2 проверять
на всех содержательных парах, а не только на сильных — оснований выделять
сильные нет.

Из K-4a4 для B1/B2 остаётся ровно одно требование, и оно про монотонность:
у оракула должно быть право отказа, router нельзя учить «чем больше, тем
лучше», `L_BCE` по changed-support не может быть основной целью.

## 7д. K-4b0: датасет для причинного router ⭐

`k4b0_build_router_dataset.py`, проверка `k4b0_verify.py`, тесты `test_k4b0.py`.
1000 наблюдений из **500 эпизодов**, 40 задач, 16 позиций вмешательства →
**16000 вмешательств**. Split по эпизодам 700/150/150 наблюдений (350/75/75
эпизодов), стратификация по задачам, каждая задача представлена в train.

**Протокол — официальный смешанный:** левый паддинг как в `eval_libero.py`,
`pos_offset` **по задаче** из `eval_libero_bar.sh` (27 задач на 4, 13 на 3),
таблица строится из самого `.sh` с хешами обоих файлов в metadata.

### Оракулы и планка для B1 (test, вся часть, кластерный бутстрап)

| способ отбора | K=1 | K=2 | K=4 | macro K=4 |
|---|---|---|---|---|
| **энтропия прохода ДО (прич.)** | 0.080 | 0.149 | **0.291** [0.263,0.318] | 0.301 |
| малый запас top1−top2 (прич.) | 0.076 | 0.153 | 0.285 | 0.296 |
| окно вокруг `p` (прич.) | 0.117 | 0.183 | 0.283 | 0.302 |
| только `p` (прич.) | 0.117 | 0.117 | 0.117 | 0.132 |
| случайно, 20 политик | 0.039 | 0.078 | 0.166 | — |
| **одиночный оракул (потолок B1)** | 0.464 | 0.613 | **0.701** [0.682,0.721] | 0.716 |
| жадный оракул | 0.464 | 0.672 | 0.846 [0.831,0.862] | 0.863 |
| точный оракул ≤K | 0.464 | 0.682 | **0.877** [0.865,0.889] | 0.891 |

val даёт то же в пределах интервалов. По всем 16000 строкам: 0.875 / 0.846 /
0.709.

**Отыгрывать надо 0.291 → 0.701** (потолок независимого scorer), а не до
жадного: обучаясь на одиночных выигрышах, top-K не может превзойти ранжирование
по истинным одиночным выигрышам. Жадный и точный — ориентиры для B2.

**Особенность, важная для B1.** При K=1 неопределённость **проигрывает**
тривиальному «только `p`» (0.080 против 0.117), а при K=4 выигрывает вдвое
(0.291 против 0.117). Значит причинный сигнал не острый, а размазанный: он не
указывает лучшую позицию, но помогает покрыть набор. Ранжирующая часть потерь в
B1 важнее регрессионной.

### Обратимость НЕ оправдана ⭐

| | test | val |
|---|---|---|
| ADD до упора (не пара для сравнения) | 0.846, размер 3.72 | 0.855, размер 3.71 |
| **ADD+STOP (честная пара)** | 0.843, размер 3.54 | 0.853, размер 3.54 |
| **ADD/SWAP+STOP** | 0.849, размер 3.54 | 0.858, размер 3.54 |
| прирост | **+0.0055** [0.0037, 0.0074] | +0.0049 [0.0031, 0.0072] |
| при равном числе позиций | +0.0055 [0.0038, 0.0074] | +0.0049 [0.0031, 0.0072] |
| строго лучше / хуже | 6.25% / 0.00% | 5.88% / 0.04% |

Прирост реален — интервал не включает ноль, хуже не стало практически нигде, —
но **в 5.5 раза ниже порога плана (+0.03)**, и весь интервал лежит под ним.
Размеры наборов совпадают, поэтому matched-cost ничего не меняет.

Состав траекторий: SWAP в 6.03% строк, **самостоятельный REMOVE в 0.01%**
(около двух строк на 16000) — возможен, как показывает детерминированный
контрпример, но пренебрежим.

**Слово `Reversible` из названия метода убирается.** Механизм остаётся в коде
и в разметке; его место — фаза D, где шагов потока много и позиция может
устареть между шагами.

История вывода: первый раз он был получен на процедуре БЕЗ обмена (0.24% REMOVE)
и справедливо отозван как артефакт. Теперь процедура включает обмен, сравнение
идёт с `ADD+STOP` при равном размере, выборка полная — вывод тот же.

### Факты постановки

| величина | train | val | test |
|---|---|---|---|
| вредных позиций внутри support | 35.0% | 34.9% | 36.4% |
| «ремонтировать нечего» | 0.36% | 0.88% | 0.58% |
| средний размер лучшего набора | 3.74 | 3.69 | 3.69 |

Размер лучшего набора при бюджете 4: 0 — 0%, одна — 2%, две — 5%, три — 9%,
**четыре — 83%**. `K_practical` (наименьший набор в пределах `tau`) даёт то же
распределение; при допуске потери 1% четыре нужны в 80%.

**Адаптивный `STOP` при официальном протоколе оправдан слабо**: ранняя
остановка почти никогда не выгодна. Зато **треть изменившихся позиций вредна**,
то есть решает качество отбора, а не размер бюджета.

«Ровно K» против «≤K» — проверено **на всех 16000 строках** векторно, через
`popcount(support)` и `best_size_by_k`: нарушений **0**. Прежде проверка шла по
подвыборке 3000 строк, и вывод распространялся на весь датасет без основания.

Запас `16 − |C| − (K − |S*|)` в среднем 8.68, но **минимум равен 0**: есть
строки, где добивать нечем в обрез. Тождественность здесь — свойство ЭТОГО
датасета при K=4, а не теорема; при большем бюджете или более крупном support
она может нарушиться, и «ровно K» будет вынужден брать вредные позиции внутри
support. Распределение запаса: 0–2 у 1.1% строк, 8 и более у 66.4%.

**Changed-support в среднем 7.05 позиции из 16**, свободных вне него 8.95.
Прежняя оценка «≈9–10 из 16» была ошибкой чтения собственного вывода: 8.95 —
это свободные позиции, а не support. Средняя длина сжатой таблицы 214.9 набора
вместо 2517; восстановить средний support из неё напрямую нельзя — число
подмножеств нелинейно по `|C|`, и распределение с тяжёлым хвостом. Сжатие
`G(S) = G(S∩C)` сверено с полным перебором: расхождение 4.7e-10 при пороге 1e-8.

### Гарантии

Побитовая повторяемость при одинаковых настройках; независимость содержания от
размера батча; канонический порядок строк; физическое разделение признаков и
меток тремя файлами с белым списком; контекстные скрытые состояния позиций
действия из уже состоявшегося прохода (16000 × 16 × 768, float16); повторное
открытие записанных файлов со сверкой ключей, числа строк и хешей; проверка
локальной оптимальности каждой обратимой траектории.

## 7е. Протокол: решает pos_offset, а не паддинг ⭐

`experiments/k4b0_padding_probe.py`, 96 наблюдений из 48 эпизодов, пять режимов
на одних и тех же данных. Имя режима = длина_сторона_offset.

### Расхождение с опорным `dyn10_l_4` (ближайший к `scripts/eval_libero.py`)

| режим | z_ref совпал | Jaccard support | Спирмен | Jaccard top-4 | перенос |
|---|---|---|---|---|---|
| `nat_l_4` | **100%** | 1.000 | 1.000 | 1.000 | **100%** |
| `nat_r_3` | 0% | 0.302 | 0.094 | 0.260 | 42.5% |
| `dyn10_l_3` | 0% | 0.302 | 0.094 | 0.260 | 42.5% |
| `fix181_r_3` | 0% | 0.317 | 0.076 | 0.254 | 45.3% |

«Перенос» — какую долю опорного одиночного оракула даёт четвёрка, выбранная в
чужом режиме; это прямой ответ на вопрос, переносится ли router.

### Механизм

`_build_joint_position_ids` даёт VLM-позиции `[0..vlen−1]`, токены действия
начинаются с `base_pos = vlen`. При **левом** паддинге настоящие токены
занимают последние `L` позиций и кончаются на `vlen−1`, поэтому разрыв до
токенов действия **равен нулю при любом объёме паддинга**. При правом —
настоящие токены в начале, разрыв равен `vlen − L` и гуляет вместе с составом
батча.

Отсюда тождества, наблюдаемые в замере: `nat_r_3 ≡ dyn10_l_3` (батч 1 без
паддинга совпадает с левым паддингом батча 10) и `nat_l_4 ≡ dyn10_l_4`.

**Левый паддинг эквивалентен отсутствию паддинга**, поэтому группировка
наблюдений по длине не нужна — достаточно `padding_side="left"`.

### Что на самом деле решает

| протокол | точный | жадный | одиночный | энтропия | запас |
|---|---|---|---|---|---|
| `pos_offset = 3` (все прежние замеры) | 0.941 | 0.921 | 0.789 | 0.417 | 0.424 |
| `pos_offset = 4` | 0.872 | 0.842 | 0.706 | 0.295 | 0.286 |

При офсете 4 задача труднее, а причинные признаки слабее. Структура
сохраняется: разрыв жадного над одиночным 0.136 против 0.132.

> ⚠️ **Единый офсет 4 — НЕ официальный протокол.** `scripts/eval_libero_bar.sh`
> задаёт офсет ОТДЕЛЬНО КАЖДОЙ ЗАДАЧЕ каждого suite: `goal (4 4 4 4 4 4 4 4 3 3)`,
> `spatial (4 4 4 4 3 3 3 3 4 3)`, `object (4 4 4 4 4 4 4 4 4 3)`,
> `10 (4 3 4 4 3 4 3 3 4 3)` — всего 27 задач на офсете 4 и 13 на офсете 3.
> Обе строки таблицы выше суть АБЛЯЦИИ. Итоговые планки берутся со смешанного
> протокола, таблица строится `k4b0_offset_table.py` из самого `.sh`.

### Что это значит для прежних замеров

Фаза A, K-4a4 и первая сборка B0 шли на `pos_offset = 3`. Внутренне они
согласованы, и качественные выводы (разреженность, провал независимого
ранжирования, немонотонность, отсутствие субмодулярности) от офсета не зависят —
структура воспроизводится. Но **абсолютные числа относятся к протоколу, который
не совпадает с официальным evaluation**, и в статье их надо либо пересчитать,
либо явно оговорить.

Отдельно отзывается прежнее объяснение расхождения между `--batch 16` и
`--batch 8`: там дело было в правом паддинге до максимума в батче, а не в
«длине паддинга вообще». При левом паддинге такого расхождения нет по
построению.

## 7ж. K-5a: кэш префикса BAR и разложение вызова политики ⭐

**Дата:** 21 авг 2026. **Как:** `experiments/k5a_prefix_cost_probe.py`,
`experiments/k5a_pipeline_bench.py`, Tesla V100-SXM2-32GB, **float16**,
batch 1, пять серий с перемешанным порядком режимов.

### Префикс кэшируем — доказано

`bar.py:902` ставит `-inf` на блок «запросы VLM -> ключи действия», строка 907
делает VLM causal внутри себя. Значит башня VLM зависит только от
`vlm_inputs_embeds`, маски паддинга и позиционных id префикса, а все три
блочных прохода получают их одинаковыми: **пересчитывается вся башня целиком,
вместе с MLP**, а не только K/V.

Проверено эмпирически, а не чтением маски: два прохода с РАЗНЫМИ токенами
действия, послойная сверка скрытых состояний VLM. **Расхождение ровно ноль по
всем 24 слоям.** Прототип с кэшем даёт **побитово те же логиты** на всех трёх
блоках (`0.000e+00`), что ожидаемо: порядок редукции в матричных произведениях
совпадает, ключи и значения те же.

### Приз измерен

```
ускорение блочной части: медиана 1.383x, диапазон [1.326, 1.543]
блок 2: 46.1 -> 25.8 мс,  блок 3: 46.2 -> 25.9 мс
```

### Оценка 1.84x из §7б СНЯТА

Она выводилась из подгонки `t(q) = 62.39 + 0.2149*q` в предположении, что весь
свободный член — башня VLM. Прямое измерение разложило проход почти пополам:

| часть блочного прохода | мс |
|---|---|
| башня действия (кэшированный проход) | 25.8 |
| башня VLM (разность с опорным) | 23.6 |
| **избыточно пересчитывается дважды** | **47.2** |

В свободный член попала **фиксированная стоимость экспертной башни** — 24 слоя
при hidden 2048, не зависящая от числа токенов и от кэша не выигрывающая.

### Разложение полного вызова политики

Длина префикса **по факту 171** токен (в §7б предполагалось 168). Целое
измерено отдельно, а не сложением медиан; расхождение с суммой частей 1.5%.

| часть | мс | доля |
|---|---|---|
| процессор (CPU: кроп, шаблон, токенизация) | 15.6 | 7.9% |
| префикс VLM (зрение + склейка эмбеддингов) | 22.3 | 11.3% |
| блоки 1–3 | **147.0** | **74.3%** |
| декод ActionCodec | 10.0 | 5.0% |
| **весь вызов** | **197.8** | 100% |

Потолки при `f = 0.748`: ускорение блоков в 1.383x даёт сквозные 1.261x, в 2x —
1.598x, бесконечное — 3.974x. Измеренный кэш даёт **сквозное 1.25x**.

### Поправка к прежнему утверждению о доле action-части

Ранее из наклона подгонки `0.2149 мс на токен` делался вывод, что вычисления на
токенах действия стоят ~3.4 мс на проход, доля после кэша `f ~ 0.07`, и потому
адаптивное число блочных проходов даёт около 1.02x. **Неверно примерно
всемеро.** Наклон описывает лишь ПРЕДЕЛЬНУЮ добавку на токен; фиксированная
стоимость экспертной башни в него не входит и составляет ~26 мс независимо от
числа токенов (16 или 48). После кэширования префикса экспертная башня занимает
БОЛЬШЕ ПОЛОВИНЫ вызова.

### Разброс машины

Опорные времена в двух прогонах одного кода разошлись на 20–25%, разброс по
сериям 13–16 мс при медиане 46–49. **Абсолютные величины для заявлений
непригодны**; годятся только отношения, посчитанные внутри серии с
перемешанным порядком режимов. Разброс целого вызова заметно меньше — 10.4 мс
на 197.8, то есть 5%.

### dtype: весь прежний конвейер шёл во float32

`from_pretrained` ИГНОРИРУЕТ запрошенный dtype — в логе builder значится
«dtype модели по факту: torch.float32 (запрошен bfloat16)». Значит датасет
`k4b0_v2` и все времена §7б относятся к float32. K-5a — первый запуск в
режиме, который имеет смысл называть развёртыванием; dtype приводится
принудительно после загрузки.

---

## 7з. K-5b: стенд LIBERO и его проверка ⭐

**Дата:** 21 авг 2026. **Как:** `experiments/k5b_fixed_horizon_eval.py`,
`experiments/k5b_harness_check.py`.

### Три дефекта стенда, найденные и исправленные

**Официальный `--ensemble False` не выключает усреднение.** `type=bool` в
argparse превращает непустую строку в `True` — проверено, выключает только
пустая. То есть все опубликованные прогоны шли с усреднением, и иначе
запустить их нельзя без правки кода.

**Загрузка модели до создания сред вешает процесс.** `AsyncVectorEnv` форкает
подпроцессы; после инициализации CUDA в родителе это дедлок. Официальный
`eval_libero.py` создаёт среды на строке 70 и грузит модель на 88 — порядок не
случаен.

**Состояние протекает между конфигурациями.** Среды переиспользовались всеми
ячейками, а `envs.reset` восстанавливает состояние не полностью. Обнаружено по
нарушению тождества: при `H = 20` (длина чанка) каждый момент времени покрыт
ровно одним планом, усреднение обязано быть пустой операцией, а `ens=on` и
`ens=off` давали разный успех. Одна и та же ячейка дала 10 успехов будучи
восьмой в процессе и 9 будучи первой — при полностью детерминированной среде
(два одинаковых запуска совпали посимвольно). Лечится **одной конфигурацией на
процесс**, как в официальном скрипте.

### Проверка на завышение успеха — стенд чист

`k5b_harness_check.py`, задача `long/0`, 10 сред, след по каждому шагу:

```
воспроизводимость сброса: 10/10 отпечатков совпали
моменты первого done: [267, 303, 228, 589, 278, 589, 267, 231, 259, 257]
переворотов done (True->False): 1
приращений награды ПОСЛЕ завершения: 2621
возвратов наблюдения к начальному: 0
ОЖИВЛЕНИЙ (награда НЕуспешной среде): 0
успех в момент первого done: 8/10;  успех по накоплению за раунд: 8/10
```

**Приращения награды после `done` дефектом НЕ являются**: LIBERO выдаёт награду
каждый шаг, пока цель продолжает выполняться, а `done` — предикат «цель сейчас
достигнута», а не защёлка. Уже успешная среда набирает единицы до конца раунда,
и после `clip(reward + r, 0, 1)` это ничего не меняет. Завышение возможно
только при ОЖИВЛЕНИИ — награде среде, неуспешной на момент своего завершения;
таких ноль, и две независимые оценки успеха совпали.

Первая версия проверки считала тревогой сам факт награды после `done` и подняла
ложный сигнал. Порог исправлен.

**Семантику учёта менять нельзя**: `reward = np.clip(reward + reward_tmp, 0, 1)`
и `while not np.all(done)` взяты из `eval_libero.py:241` и `154`. Наши числа
сопоставимы с опубликованными только пока мы её воспроизводим.

Ограничение проверки: возврат наблюдения ловится только к СВОЕМУ начальному
состоянию; перезапуск с другим начальным состоянием остался бы незамеченным.

### Усреднение — конфаунд развёртки по горизонту

`ActionEnsembler` копит предсказания по абсолютным моментам времени и усредняет
их. Глубина усреднения **обратна горизонту**:

| H | чанков в среднем | возраст старейшего |
|---|---|---|
| 4 | 5 | 16 шагов |
| 8 | 2.5 | 12 |
| 12 | ~1.7 | 8 |
| **20** | **1** | усреднение выключается само |

Поэтому сравнивать горизонты только при `ens=on` нельзя: падение успеха
смешивается с потерей сглаживания. Меряются оба режима.

### Предварительные числа, 10 эпизодов на ячейку

LIBERO-goal, задачи 0–2: успех 29–30 из 30 во всех восьми ячейках, зависимости
от горизонта нет. **Потолок** — отличить 100% от 97% на десяти эпизодах
невозможно.

LIBERO-10 (long), задачи 0–3, по 40 эпизодов на ячейку:

| вызовов/действие | H | без усреднения | с усреднением |
|---|---|---|---|
| 0.25 | 4 | 90.0% | **100.0%** |
| 0.125 | 8 | 95.0% | 95.0% |
| 0.084 | 12 | **97.5%** | 92.5% |
| 0.050 | 20 | 92.5% | 92.5% |

Тождество при `H=20` выполняется **поточечно** (100/90/90/90 в обоих режимах) —
это встроенная проверка исправности стенда.

Читается пока так: официальный протокол (`ens=on`, `H=4`) — лучший;
пятикратное сокращение вызовов стоит 7.5 пункта; усреднение работает только на
коротком горизонте (+10 пунктов при `H=4`, ничего при `H=8`). Разница 100%
против 92.5% — три эпизода при стандартной ошибке около 4 пунктов, то есть
1.8 сигмы. **Указание, а не результат**; нужен полный протокол на 50 эпизодах.

Тайм-ауты чужие, не наши: `steps` считается после десяти ожидательных шагов,
590 означает 600 полных шагов среды (предел LIBERO для long), 290 — 300 (для
goal). Наш `--max-steps 600` ни разу не достигается.

---

## 8. Закрытые направления

Перечислены, чтобы не переоткрывать.

- **Локальность исследования у дискретных политик.** Управление масштабом
  достаётся бесплатно температурой по уровням (1.86×); голова добавляет сверх
  этого только локальность (1.65×), и то на VQ-VLA. На OAT политика уже
  локальна (13–24% пути), запас ~1.2×. Мало.
- **Геометричное ядро переходов в дискретном потоке.** Занято DFM-VLA:
  `p_t(x|x₁) = softmax(−β_t·d(x,x₁))` плюс Metric-Aligned Action Tokenizer,
  обучаемый триплетным лоссом. Абляции: LIBERO 98.0 полная / 96.7 без MAAT /
  93.5 без embedding-guided.
- **Иерархическая генерация над RVQ сама по себе.** Занято ResGen,
  SIEDD/HiCoDD, HiCoDiT, ActionCodec-BAR — все сохраняют иерархию через
  фиксацию грубого уровня.
- ~~**Согласованный блочный переход.**~~ Был объявлен закрытым по первой
  версии K-1. **Снято**: исправленный замер (§2) показал эффект 1.39 на
  грубом уровне. Направление живо, но формулировать надо как утверждение о
  цене, а не о «починке иерархии», и сперва развести конфаунд с направлением
  смещения.

## 7и. Литературная обстановка: направление адаптивного горизонта занято ⭐

**Дата:** 22 авг 2026. **Как:** проверка ссылок через arXiv, не по пересказу.

Три работы занимают ровно ту постановку, к которой мы пришли. Все проверены
по первоисточнику; две из них датированы позже моей границы знаний, поэтому
сверялись отдельно — цена ошибки здесь закрытие ветки из-за несуществующей
статьи.

| работа | что адаптирует | сигнал |
|---|---|---|
| [AAC](https://arxiv.org/abs/2604.04161), CVPR 2026 | длину чанка | энтропия действий |
| [AutoHorizon](https://arxiv.org/abs/2602.21445), фев→июн 2026 | горизонт исполнения | веса action self-attention |
| [VLA-Corrector](https://arxiv.org/abs/2607.01804), июл 2026 | обрыв чанка | расхождение визуальной динамики |

**AutoHorizon бьёт точнее всех.** Их наблюдение — «varying the execution
horizon leads to substantial performance deviations, with performance initially
improving and then declining as the horizon increases» — дословно наша
развёртка §7з (91.2 → 92.0 → 90.8 → 85.0). Бенчмарк тот же: четыре suite
LIBERO, включая LIB-10, плюс RoboTwin и реальный робот. Заголовок «VLA Knows
Its Limits» — это формулировка, к которой мы пришли независимо.

Их базовые линии перекрывают почти весь наш план экспериментов: `Static Oracle`
(фиксированные горизонты `e ∈ {0.2p…1.0p}`), `Static Oracle+` (перебор,
«requires p rollouts per task»), `Random` (`e ~ U(1,p)` на каждом шаге),
`Action Trigger` (первый индекс, где разность соседних действий превышает
`τ_a ∈ {0.01,0.05,0.1,0.3}`), `Uncertainty Proxy` (дисперсия по 4 сэмплам,
`τ_u` из того же набора).

**Их же числа подтверждают наше арифметическое возражение.** LIB-10, π₀.₅:

| | успех |
|---|---|
| Static Oracle `e=0.6p` (плохо выбранный) | 82.4 ± 2.0 |
| **Static Oracle+ (перебор фиксированных)** | **91.9 ± 0.4** |
| AutoHorizon | 92.1 ± 1.0 |

Над **лучшим фиксированным** адаптивность даёт 0.2 пункта. Весь заявленный
выигрыш — над плохо выбранным фиксированным. Значит правильный эталон для
любого адаптивного метода — перебор фиксированных горизонтов, а Pareto против
`H=4` читается как подтасовка.

**Чего они не делают.** Вызовы политики не отчитываются вообще («no separate
policy call count metric reported»), накладные расходы описаны как
«negligible». Ось «успех против компьюта» свободна.

### Что остаётся нам

Статья-метод закрыта. Остаётся аудит, и он опирается на одну вещь: **и AAC, и
AutoHorizon — неконтролируемые прокси без ground truth.** AutoHorizon прямо:
«does not use an explicit ground-truth signal… no supervised training phase».
Величина, которую они оценивают, никем не измерена. Наш зонд §7к её меряет.

### Арифметика запаса: против чего вообще можно выиграть

Пусть `q` — доля времени под коротким горизонтом, остальное под `H=20`.
Вызовов на действие `q/4 + (1−q)/20`. Безубыточность против фиксированного
`H=8` (0.126) при `q = 0.38`, против `H=12` (0.085) при `q = 0.17`.

**Если хотя бы каждый шестой момент требует короткого горизонта, адаптивность
не обгоняет тривиальный фиксированный `H=12`.** Потолок затеи — около 2.5× над
`H=8`, из которых 1.5× берётся бесплатно переходом на `H=12`.

## 7к. K-5c: инструмент прямого измерения устаревания

**Дата:** 22 авг 2026. **Как:** `experiments/k5c_drift_probe.py`,
`experiments/run_k5c_drift.sh`. Самопроверки проходят, прогона ещё не было.

Меряется `D(j) = d(C_t[j], C_{t+j}[0])` — расхождение устаревшего предсказания
со свежим. **`D(0) ≡ 0` тождественно**, потому что при `j = 0` оба действия
получены из одного состояния тем же жадным декодером (`eval_libero.py:207`,
`do_sample=False`; `bar.py:1699` — argmax). Отсюда два следствия.

**Хорошее:** `D` — чистое устаревание, ошибки генерации в нём нет по
построению.

**Плохое:** разделить «плохо сгенерировала» и «устарело» этим инструментом
нельзя, для связи с исходом нужна отдельная калибровка ветвлением.

### Позиционный конфаунд и тёплицевость

`C_t[j]` против `C_T[0]` — это позиция `j` одного чанка против позиции `0`
другого. Профиль декодера по позициям у нас неоднороден (§7б), поэтому рост
`D(j)` может оказаться артефактом позиции.

Различающая статистика: **чистое устаревание зависит только от разности
свежестей `|j_a − j_b|`**, то есть полная попарная матрица расхождений между
всеми живыми происхождениями постоянна вдоль диагоналей. Позиционные эффекты
дают структуру сверх разности. `toeplitz_deviation` возвращает долю дисперсии,
не объяснённую наилучшим тёплицевым приближением.

Проверено на подставных данных: при устаревании `α·j` отклонение `< 1e-9`;
при нулевом устаревании и особой позиции 0 (`b(0)=0.5`) наивная кривая лжёт
ровно `0.5·√6 = 1.22`, а отклонение подскакивает до **0.90**.

Матрица копится **только когда живы все двадцать происхождений**. Иначе у пар
разные множества наблюдений — пара `(0,19)` видна лишь с `T ≥ 19`, а `(0,1)` с
`T ≥ 1` — и зависимость дрейфа от фазы задачи протекает в тест как ложная
непёплицевость. При полном наборе множество `(T, среда)` у всех пар одно и то
же, и любая зависимость вида `g_T(лаг)` сокращается при усреднении.

### Предел теста, названный явно

**Линейное позиционное смещение тёплицево:** `‖b(j_a) − b(j_b)‖ =
|j_a − j_b|·‖наклон‖`. Такой артефакт **неотличим** от линейного устаревания, а
линейный рост — ровно то, что мы ожидаем увидеть. Самотест 4c это фиксирует:
линейный артефакт даёт отклонение `< 1e-9` и имитирует кривую один в один.

Первая версия самотеста ловила только ступенчатое смещение — самый лёгкий
случай, — и на этом основании тёплицевость была объявлена достаточным
контролем. Это было неверно.

Закрывается только там, где устаревание нулевое по построению: **статическая
сцена** (`--static-steps`). Пока рука стоит от холостых действий, наблюдение
почти не меняется, и всё измеренное `D(j)` — позиционный пол. Сцена не идеально
статична (схват и физика доседают), поэтому это **верхняя** оценка пола.

### Правило чтения, записано до запуска

- `D(0) = 0` **ничего не проверяет**: свежее берётся как `chunk[:, 0, :]`, и при
  `j = 0` сравнивается с тем же самым массивом. Это тождество индексации.
  Настоящая проверка — `repeat_call_maxdiff`: политика вызывается дважды на
  ОДНОМ наблюдении, и разница обязана быть строгим нулём. Если нет, часть
  `D(j)` — шум повторного вызова, и это второй пол наравне со статическим.
- **Без измеренного пола рост `D(j)` называть устареванием нельзя вообще.**
- `D` заметно выше пола и отклонение от тёплицевости `< 0.15` — устаревание
  реально. Тёплицевость здесь необходима, но не достаточна.
- Отклонение велико — есть позиционная структура сверх разности свежестей.
- `D` близка к полу или плоская — устаревания на этом горизонте нет, ветку
  закрывать.

Отдельно из сохранённых строк считается `D` против **абсолютного времени** `T`:
если дрейф объясняется фазой эпизода, предиктор выучит «шаг 300 → H=8», и это
не горизонт от состояния. Поля `T`, `round`, `env` для этого уже пишутся,
дополнительного прогона не нужно.

### Kill criteria следующих фаз, оцифрованы заранее

| # | критерий | порог провала |
|---|---|---|
| 1 | связь дрейфа с исходом | AUC предсказания провала по `D` < 0.60 |
| 2 | предсказуемость дрейфа | `R²(метки) − R²(перемешанные)` < 0.05 |
| 3 | превосходство над прокси | не бьём энтропию и Action Trigger с зазором > 0.03 R² |
| 4 | Pareto | не даём < 0.085 вызова на действие при успехе ≥ 92% |

Четвёртый — фиксированный `H=12`, который берётся бесплатно.

### Ограничения инструмента

Усреднение выключено всегда: `ActionEnsembler` смешал бы планы разной свежести
именно в измеряемой величине. `Action Trigger` воспроизведён по описанию
AutoHorizon, но метрика и нормировка у них не указаны — наша интерпретация
(L2 по позе в нормализованном пространстве) должна называться таковой.
`Uncertainty Proxy` требует сэмплирования и в зонд не входит.

## 7л. K-5c: результаты зонда устаревания, 9 задач из 10 ⭐

**Дата:** 22–23 авг 2026. `data/k5c_drift`, LIBERO-10, `H_exec = 20`,
20 эпизодов на задачу, вызов политики каждый шаг.

### Стенд

Детерминизм строгий во всех раундах: повторный вызов на одном наблюдении даёт
`max|Δ| = 0.000e+00`. Отклонение от тёплицевости 0.060.

**`D(0) = 0` ничего не проверяет** — свежее берётся как `chunk[:, 0, :]`, при
`j = 0` сравнивается с тем же массивом. Это тождество индексации. Настоящая
проверка — повторный вызов, добавлена отдельно.

### Чанк несёт настоящую временную структуру

Базлайн `D_держать` = повторять первое действие плана `j` шагов:

| j | план | держать | план ловит |
|---|---|---|---|
| 1 | 0.169 | 0.181 | 5% |
| 8 | 0.250 | 0.484 | 48% |
| 19 | 0.319 | 0.840 | **62%** |

План выигрывает на всех девятнадцати смещениях, отрыв растёт. При больших `j`
«держать» ≈ уровень случайности. Гипотеза «политика просто шумит» опровергнута.

**Обрыва на конце чанка нет:** на `j=19` план всё ещё втрое точнее случая.
Значит `H = 20` — потолок ДЛИНЫ ЧАНКА, а не измеренная граница пригодности.

### Главный гейт провален

Ранний дрейф (первые 100 шагов) против провала эпизода, 24 провала из 180:

| метка | AUC общий | AUC внутри задач | нуль 95% |
|---|---|---|---|
| cosdef | 0.714 | 0.642 | 0.368–0.632 |
| cum | 0.613 | 0.656 | 0.371–0.632 |
| d_mean | 0.599 | 0.642 | 0.366–0.632 |

**Порог Бонферрони (0.678) не проходит никто, ни разу за четыре замера.**
Внутризадачный AUC держится около 0.65 — формально выше предзарегистрированного
0.60, но поправку на пять метрик не выдерживает.

Стратификация обязательна: общий AUC может целиком объясняться тем, что у
трудной задачи дрейф выше, а межзадачная неоднородность лечится таблицей из
десяти чисел. Самотест подтверждает, что стратификация снимает чисто
межзадачный сигнал (0.751 → 0.489).

### Снятые утверждения

**«Несогласие на первом шаге предсказывает провал».** На восьми задачах связь
`ratio(j=1)` с успехом держалась и без выброса (`−0.773` при критическом
`0.754`). На девяти — `−0.639`, ниже критического `0.666`. Задача 8, вторая
трудная (80%), дала на `j=1` обычные 0.98.

**Единой величины «срок годности плана» не существует.** Трудные задачи трудны
по несовместимым причинам: задача 4 несогласована на первом шаге
(`j=1` = 1.20 при 0.93–1.06 у прочих), задача 8 быстрее всех стареет на дальних
(`j=19` = 0.54, максимум по набору). Одной сводной величиной это не
описывается.

**Статический контроль меряет не то.** В статической фазе робот не исполняет
план, поэтому `C_{t+j} ≈ C_t` и измеряется `‖C_t[j] − C_t[0]‖` —
внутричанковая изменчивость. Пол по завершённым эпизодам даёт то же по той же
причине (робот неподвижен). **Чистого позиционного контроля построить не
удалось.** Линейный позиционный артефакт тёплицевость не ловит по построению
(самотест 4c).

## 9. Новое направление: токенизатор, эквивариантный к сдвигу окна ⭐

**Дата:** 23 авг 2026. Гипотеза, экспериментов ещё нет.

### Улика

`eval_libero_bar.sh` задаёт `pos_offset` ОТДЕЛЬНО для каждой из сорока задач
(27 задач с 4, 13 с 3). Это временная **фаза** позиционного кодирования, и она
не косметическая: `k4b0_padding_probe` показал, что между офсетами 3 и 4
меняется сам план BAR, оракул расходится 0.941 против 0.872 (§7е).

**Представление действий зависит от фазы начала окна, и авторам пришлось
подбирать фазу руками под каждую задачу.**

### Диагноз

Кодек берёт окно из 20 шагов и кодирует его ЦЕЛИКОМ в 16 временных позиций
через RVQ; декодер получает сумму трёх уровней (§1). Сдвиг окна на шаг означает
полную перекодировку. Гарантии, что действие `a_{t+j}` получит одно и то же
представление в окне из `t` и в окне из `t+j`, нет.

Отсюда следует вся картина костылей в литературе:

| симптом | чей костыль |
|---|---|
| перекрывающиеся планы не совпадают → усредняем | temporal ensembling (ACT) |
| поправка зависит от позиции внутри чанка | A2C2 подаёт индекс позиции на вход |
| новый чанк рассогласован с исполняемым | RTC согласует на инференсе |
| фаза влияет на качество | ручной `pos_offset` под задачу |
| `D(1) = 0.169` | наш замер §7л |

**ПОПРАВКА 23 авг, важная.** Утверждение «причина не названа ни в одной
работе» ОШИБОЧНО. Статья самого ActionCodec (arXiv 2602.15397, Dong, Liu,
Zhang и др., фев 2026) ставит эту причину ПЕРВЫМ из четырёх принципов дизайна:
«Maximized Temporal Overlap (Overlap Rate): обеспечивает согласованность между
соседними чанками; меряется как доля токенов, остающихся идентичными между
перекрывающимися во времени сегментами; больший overlap снижает artifact
entropy — стохастические скачки токенов от малых возмущений действия». Есть и
отдельный этап обучения ради этого: «RVQ post-training замораживает первичный
кодбук, чтобы сохранить overlap rate при снижении ошибки реконструкции».

Мы несколько недель мерили эту модель, не прочитав её статью. Это должно было
быть первым действием, а не третьей проверкой литературы.

Что после поправки остаётся (всё намного скромнее):

- **Их метрика — в пространстве ТОКЕНОВ** (доля совпавших токенов), наша — в
  пространстве ДЕЙСТВИЙ (L2 и косинус декодированных действий). Величины
  расходятся: совпадение токенов достаточно, но не необходимо для совпадения
  действий. Наш замер D(1)=0.169 показывает, что у токенизатора, специально
  спроектированного под overlap, остаточное расхождение действий составляет
  27% от величины действия и 25° по направлению. Это критика метрики, а не
  открытие причины.
- **Противоречие между заявленным дизайном и выпущенным кодом.** Статья
  заявляет «Temporal Window: fixed 1-second encoding with timestamp-based
  positional awareness», то есть позиционирование по абсолютному времени. Но
  `eval_libero_bar.sh` вручную подбирает `pos_offset` под каждую из сорока
  задач, и замер k4b0_padding_probe показал, что этот выбор МЕНЯЕТ ПЛАН
  (оракул 0.941 против 0.872). Если позиционирование по абсолютным меткам
  времени, ручная фаза не должна была бы ничего менять.
- **Каузальный потоковый кодек для ДЕЙСТВИЙ, судя по всему, не сделан.** Их
  архитектура — Perceiver с cross-attention по фиксированному окну в секунду,
  не потоковая. Но для движения человека это уже есть: «Causal Motion Tokenizer
  for Streaming Motion Generation», ICCV 2025 W, RVQ с каузальными свёртками.
  То есть перенос, а не изобретение, и мотивация «никто не видел проблемы»
  отпала.

### Предложение

Требование: код, отвечающий за абсолютный момент `τ`, не зависит от начала
окна. Следствия, все проверяемые готовыми инструментами:

- перекрывающиеся чанки совпадают тождественно → усреднение не нужно;
- `pos_offset` исчезает как гиперпараметр;
- длинный горизонт безопасен по построению;
- `D(1)` обязан упасть к ошибке реконструкции (мерит `k6a`).

**Механизм — каузальный потоковый кодек:** код момента `τ` зависит только от
`a_{≤τ}`, эквивариантность точная. Так устроены аудиокодеки (SoundStream,
EnCodec); для действий этот перенос, судя по всему, не сделан.

**Ключевой аргумент, которого нет у других:** каузальность стоит токенов, и
обычно это останавливает. Но §7б измерил, что число токенов действия почти не
влияет на время — потолок сокращения `1.055×`, доля позиций при q=16 равна
5.2% от вызова. **Мы можем позволить себе тратить токены.**

### Риски

- Переучивать придётся и голову VLA: коды меняются, LoRA не хватит. Токенизатор
  при этом мал и учится на действиях, без картинок.
- Каузальность может стоить реконструкции: некаузальный кодек видит окно
  целиком. Проверяется до VLA — обучить кодек, померить реконструкцию и `k6a`.
- Литературу проверить ДО начала: «shift-equivariant / streaming / causal
  action tokenizer». Обожглись уже дважды.

### Порядок

1. `k6a_quant_floor.py` на текущем кодеке — сколько несогласованности он даёт
   сам. Час, без GPU. Правило чтения: доля от политических `0.169` больше ~60%
   — виноват токенизатор; меньше ~25% — виновата политика.
2. Литературный поиск.
3. Обучить каузальный кодек на действиях LIBERO, померить реконструкцию и
   `k6a`. Дни, маленькая сеть.
4. Только при хорошем п.3 — переучивать голову VLA и мерить успех против
   вызовов.

## 10. K-6b: три прохода BAR нужны ради вычислений, а не ради связи уровней ⭐

**Дата:** 23 авг 2026. `experiments/k6b_delta_h.py`, 6000 наблюдений, 600
эпизодов, разбиение по эпизодам 4200/900/900, три сида, вывод в
`data/k6b_delta_h.{json,log}`.

### Что меряли

BAR тратит три полных прохода по башне (147 из 195 мс, 75% вызова), потому что
RVQ последовательна. Вопрос: сколько стоит отказ от обусловливания на
предыдущих уровнях.

```
dCE_g = CE(k_g | h, NULL) − CE(k_g | h, настоящие k_<g)
```

`h` — вход `action_lm_head` (`bar.py:1248`) на ПЕРВОМ блоке, то есть всё, чем
располагала бы однопроходная архитектура.

### Зависимость ничтожна

| уровень | NULL−TRUE | доля от ln(2048)=7.62 | контроль SHUF−TRUE |
|---|---|---|---|
| 1 | +0.050 нат | 0.7% | +0.046 |
| 2 | +0.061 нат | 0.8% | +0.059 |

**Две независимые базы без информации сошлись** (NULL и перемешивание) — это и
делает измерение доверенным. На дымовом прогоне контроль был вырожден
(SHUF−TRUE = +0.004 при NULL−TRUE = +0.127): перестановка внутри задачи при
малых группах почти тождественна. Теперь доля реально сдвинутых записей
измеряется, и при вырождении контроль расширяется на всю выборку.

### Главное: обусловливание и вычисления — разные вещи

Ошибка позы, RMS в долях размаха, на отложенных 900 наблюдениях:

| вариант | поза RMS | схват | к эксперту |
|---|---|---|---|
| эксперт (истинные коды) | 0.0142 | 0.0% | 0 |
| **BAR последовательная** | **0.0242** | 0.7% | 0.0207 |
| один проход, NULL-головы | 0.0315 | 0.7% | 0.0292 |
| дешёвые условные головы | 0.0314 | 0.7% | 0.0292 |

```
обусловливание даёт:       +0.0001 RMS   (+0.4% от BAR)
полный проход башни даёт:  +0.0073 RMS   (+30%  от BAR)
```

**Три прохода нужны не ради остаточной структуры RVQ, а ради вычислений.**
Голова здесь — двуслойный MLP на `h`, а блоки 2–3 прогоняют всю башню заново,
перечитывая изображение. Раньше эти два фактора были склеены.

Переформулировка благоприятная: параллельной схеме нужна не связь между
книгами, а более ёмкая голова, причём работающая только с действиями и не
перечитывающая картинку.

### Бюджет

```
BAR с честным кэшем башни   148.9 мс
один проход без головы       97.3 мс
на 1.4x голова должна стоить  ≤ 9.1 мс
на 1.3x                       ≤ 17.2 мс
полный проход экспертной башни  25.8 мс
шесть её слоёв из 24             6.5 мс
```

Шестислойный уточнитель укладывается и закрывает 1.4×.

### Побочно

BAR воспроизводит коды токенизатора лишь в **64.3%** случаев — значительная
часть её ошибки собственная, а не структурная.

### Снято

**«Концентрация подтверждает §A0».** Сказано по дымовому прогону
преждевременно: при `dCE = 0.05` наблюдаемая доля топ-5 позиций (0.80)
отражает в основном концентрацию ШУМА. Самотест «без зависимости» по
концентрации не проверялся. §A0 этими числами не подтверждается.

### Три ошибки переноса, стоившие трёх падений

Датасетный и средовой пути имеют РАЗНУЮ предобработку по всем трём входам, а
я переносил преобразования из `k5b`/`k5c`, где вход из среды:

- **изображение:** `[:, :, ::-1]` нужен только кадрам robosuite (BGR);
  `PIL.convert("RGB")` уже даёт RGB;
- **кроп:** доля 87.5% берётся от ФАКТИЧЕСКОГО размера
  (`k3_bar_suffix_repair.py:217`), в датасете кадр 256, а не 224;
- **состояние:** `process_state` ждёт сырые 9 измерений с кватернионом, в
  LeRobot состояние уже приведено к восьми (`k4b0:575` нормирует напрямую).

### Ограничение

Головы видят только `h` (768 × 16 позиций из первого блока). Часть информации,
которую блоки 2–3 добывают перечитыванием изображения, в `h` может
отсутствовать вовсе. Развёртка по ёмкости головы это покажет: выход кривой на
плато ВЫШЕ BAR означает упор в `h`, а не в голову.

## 11. K-6h ... K-9i: уровни отбрасывать можно, глубину — нет ⭐

**Дата:** 1–2 сен 2026. Скрипты `k6h_coarse_gate.py`, `k6h_summarize.py`
(агрегатор общий), `k9c_joint12_train.py`, `k9e_extract_h12.py`,
`k9f_readout_table.py`, `k9g_convert_rstar.py`, `k9h_multiarm_gate.py`,
`k9i_latency.py`. Вывод в `data/k9d_*`, `data/k9h_direct_*`, `data/k9f`,
`data/k9i_latency.json`.

### Протокол симуляторных гейтов

LIBERO-10, H=8, `ensemble=on`, 400 парных эпизодов (10 задач x 4 блока
начальных состояний x 10 сред), одна ячейка на процесс. Сверка парности по
хешу начального наблюдения, включая камеру запястья. Точный тест Макнемара,
кластерный бутстрап по ЗАДАЧАМ, границы ОДНОСТОРОННИЕ при пороге 5 пунктов.
Правило чтения трёхстороннее и записано в докстроке ДО запуска: нижняя граница
выше −margin — не-худшесть доказана; ВЕРХНЯЯ ниже −margin — ухудшение
доказано; между ними не доказано ничего.

### Уровни: отказ бесплатен

| пара | успех | разность | одностор. границы | вердикт |
|---|---|---|---|---|
| coarse24 − fullbar, K-6h, 200 пар | 89.0 / 88.0 | +1.0 | нижняя −3.0 | не хуже |
| **coarse24 − fullbar, K-9h, 400 пар** | **90.0 / 88.5** | **+1.5** | **[−0.2, +3.2]** | **не хуже** |

Два независимых измерения, второе — прямой парой в одном процессе. Знак схвата
в исполняемом окне при отбрасывании уровней не переворачивается никогда.

### Глубина: планку не берёт

| пара | успех | разность | одностор. границы | вердикт |
|---|---|---|---|---|
| joint12 − coarse24, K-9d | 89.5 / 90.0 | −0.5 | [−3.5, +3.0] | не хуже |
| frozen12 − fullbar, K-9h | 86.5 / 88.5 | −2.0 | [−6.0, +2.0] | НЕ ДОКАЗАНО |
| frozen12 − coarse24, K-9h | 86.5 / 90.0 | −3.5 | [−7.8, +0.5] | НЕ ДОКАЗАНО |

**Не-худшесть НЕ ПЕРЕНОСИТСЯ ПО ЦЕПОЧКЕ.** Две отдельные границы −3.0 и −3.5
не складываются в утверждение против полной BAR: две пре-регистрированные
пятёрки дают в сумме десять. Прямая пара показала это прямо — frozen12 планку
не берёт, хотя цепочка дала бы ложное «не хуже».

### Латентность измерена, а не выведена

`k9i_latency.py`, 200 повторов, чередование ПО ПОВТОРУ (блоками последняя
конфигурация выглядит медленнее из-за нагрева; на модели линейного дрейфа
блоками 1.9x, чередованием <1.05x).

| конфигурация | батч 1 | ускор. | батч 10 | ускор. |
|---|---|---|---|---|
| fullbar, 24 слоя x 3 прохода | 185.9 мс | 1.00x | 566.5 мс | 1.00x |
| **coarse24, 24 x 1** | **84.7** | **2.19x** | **296.8** | **1.91x** |
| 12 слоёв x 1 | 64.5–65.1 | 2.88x | 261–262 | 2.17x |

Проверка стенда: joint12 и frozen12 расходятся на 1.0% и 0.3% — у них
одинаковые формы и операции, различимых строк три, а не четыре.

**Половина стоимости вызова глубине не подчиняется.** Из `coarse24 = F + D` и
`depth12 = F + D/2` на батче 1 следует `D ≈ 39` мс на полный проход и
`F ≈ 45` мс постоянных расходов — зрительная башня и декодер действия (11 мс,
почти не зависит от числа уровней: 11.8 против 11.2). Поэтому урезание глубины
вдвое даёт лишь **1.30x** к coarse24 на батче 1 и **1.13x** на батче 10.
Прежняя оценка 1.48x была арифметикой по числу слоёв и завышена.

### Офлайновое согласие не предсказывает успех

Таблица `k9f` (test, 14407 наблюдений; расхождение кэша h12 с живым проходом
ноль токенов из 2.4 млн, клетки с известным ответом воспроизведены с точностью
0.00 и 0.01 пункта):

| ствол | голова | согласие | поза8 | знак8 |
|---|---|---|---|---|
| исходный | исходная | 23.1% | 0.1443 | 3.51% |
| исходный | Joint-12 | 26.8% | 0.1348 | 3.08% |
| Joint-12 | исходная | 31.5% | 0.1221 | 2.61% |
| Joint-12 | Joint-12 | 32.8% | 0.1199 | 2.51% |
| исходный | R* обученная | 32.9% | 0.1209 | 2.64% |

Считыватель в 1.6 млн параметров на ЗАМОРОЖЕННОМ стволе воспроизвёл 102%
прироста от дообучения 880 млн. В симуляторе, однако, Frozen-12 дал 86.5%, а
Joint-12 — 89.5% при одинаковой опоре 90.0%: **разница в 0.1 пункта согласия
обернулась 3 пунктами успеха**.

Третье независимое подтверждение: coarse-only имел на 32% худшую реконструкцию
при нулевой потере успеха; fast12 имеет 33% согласия против 87% у полной
глубины при неотличимом успехе; здесь — обратный случай.

### Собственный шум замкнутого цикла ≈ 15%

coarse24 и fullbar дают одинаковый успех, но расходятся по ИСХОДУ на 60 парах
из 400 (**15.0%**). У пары «12 против 24 слоёв» — 14.5%.

| пара | дискордантных | оракул постфактум |
|---|---|---|
| coarse24 − fullbar (успех одинаков) | 15.0% | 96.8% |
| fast12 − coarse24, K-9d | 14.5% | 97.0% |
| frozen12 − coarse24 | 19.5% | 98.0% |

Изменение, заведомо не влияющее на успех, переворачивает исход почти так же
часто, как изменение глубины вдвое. **Оракул в 97% знает исход задним числом и
запасом адаптивной глубины НЕ является.** Чистый контроль (тот же coarse24 при
другой форме батча) ещё не проведён, но априорная поддержка сильная.

### Снятые утверждения

**«Потолок дешёвой адаптации 26.5% пробит».** Потолка не было: те 26.5%
упирались в 4000 обучающих наблюдений, а не в глубину и не в ёмкость. На 150k
тот же класс методов даёт 32.9%. Это же объясняет плато K-8b/K-8c, которое
четырежды объяснялось исчерпанием глубины.

**«Дообучение магистрали избыточно».** Держалось целиком на офлайне; в
симуляторе Frozen-12 проиграл Joint-12 три пункта.

**«Ускорение половинной глубины 1.48x».** Арифметика по числу слоёв;
измеренное значение 1.13–1.30x к coarse24.

**«50 начальных состояний LIBERO — это старты демонстраций».** Не проверялось.

**«Параметров на пример» как предиктор обобщения.** Величина ненадёжна:
каждое наблюдение даёт 16 токенных целей и полный мягкий таргет на 2048
классов, а параметры предобучены.

### Инженерные уроки

- `--k-set` ломал парность: переиспользование сред между раундами зависит от
  того, какие траектории шли раньше. Второй блок — отдельный процесс с
  `--init-start`.
- `CUDA_VISIBLE_DEVICES=1` ломает рендеринг: robosuite выводит из него
  `MUJOCO_EGL_DEVICE_ID`, а маскировка оставляет одно устройство с индексом 0.
  Распараллеливать надо через `--device cuda:N`. Число устройств, видимых EGL,
  может быть МЕНЬШЕ числа CUDA-карт.
- `[ -s "$json" ] || rm -f "$json"` (унаследовано из `run_k5b_sweep.sh`)
  удаляет файл ровно тогда, когда он и так пуст, а частично записанный
  НЕПУСТОЙ оставляет. Удалять надо безусловно при ненулевом коде.
- `${X:-по умолчанию}` подставляет умолчание и при ПУСТОЙ переменной; чтобы
  различать «не задано» и «задано пустым», нужен `${X-по умолчанию}`.
- Проверки чекпойнта обязаны стоять ДО `get_envs`: иначе сообщение об отказе
  тонет в предупреждениях robosuite от десяти процессов сред.
