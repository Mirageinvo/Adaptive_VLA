# AS-DF-VLA — Active-Set Discrete Flow VLA

Рабочий план. Прежний план по RC-HDF закрыт и перенесён в
`PLAN_RC_HDF_ARCHIVED.md`; числа оттуда не цитировать.

## Гипотеза

> После изменения текущего плана полезность повторного пересчёта сосредоточена
> в небольшом, нелокальном и зависящем от состояния множестве временных
> action-позиций. Нужно научиться причинно выбирать это множество ДО дорогого
> прохода и действительно вычислять только выбранные позиции.

**Слово `Reversible` убрано из названия по результату замера.** На полном B0
(16000 вмешательств, официальный смешанный протокол) обратимая процедура с
обменом даёт над честной парой `ADD+STOP` при равном размере набора прирост
**+0.0055** [0.0037, 0.0074] на test при пороге плана **+0.03** — в 5.5 раза
ниже, весь интервал под порогом. Механизм остаётся в коде и разметке; его
место — фаза D, где шагов потока много и позиция может устареть между шагами.

## Что установлено (feasibility evidence, не результат модели)

Всё получено оракулом, который копирует коды из `z_ref`; ни обучаемого router,
ни настоящего sparse refiner пока нет. Подробности — `FINDINGS.md` §7б, §7в.

Числа приводятся по датасету B0 (16000 вмешательств, test), потому что именно
на нём оценивается router. В скобках — фаза A (96 наблюдений). Расхождение
объясняется **единым офсетом 3** в фазе A и другой выборкой; прежнее объяснение
через длину паддинга опровергнуто зондом — при левом дополнении разрыв в
position ids нулевой при любой длине, см. §7е.

| величина | значение |
|---|---|
| changed-support | 7.05 из 16 (фаза A: 4.74) |
| точный оракул, K≤4 | **0.877** [0.865, 0.889] (0.93) |
| жадный оракул, K≤4 | **0.846** [0.831, 0.862] (0.91) |
| независимое ранжирование по истинным gains | **0.701** [0.682, 0.721] (0.79) |
| **лучший причинный baseline** (энтропия) | **0.291** [0.263, 0.318] (0.40) |
| случайный выбор четырёх, 20 сидов | **0.166** (0.18) |
| 16→4 внутри BAR | 1.03× на проход, сквозного выигрыша нет |
| вредных позиций внутри changed-support | 36.4% |
| лучший набор меньше четырёх | 17% случаев |
| прирост обратимости при равном размере | +0.0055 при пороге +0.03 — не оправдана |

Непрерывные окна и два сегмента недостаточны (тест эквивалентности провален,
верхняя граница отставания 0.042 при пороге 0.03). Отдельный action-only
refiner не проверен.

## Чего заявлять НЕЛЬЗЯ до дополнительных тестов

- «Функция ремонта доказанно субмодулярна».
- «Синергии между позициями нет».
- «Последовательный router обязательно превосходит one-shot».
- «Четырёх позиций всегда достаточно».
- «Разреженный refinement уже ускоряет VLA».
- «Offline action error переводится в success rate».
- «Структура с искусственной BAR-интервенции сохраняется на траектории потока».

Корректная текущая формулировка:

> Выбранные позиции в среднем демонстрируют избыточность, а последовательный
> оракул избегает взаимозаменяемых позиций. Субмодулярность и предсказуемость
> active set требуют отдельных тестов.

## Главный ближайший вопрос

> Можно ли из строго причинных признаков выбрать не более четырёх позиций так,
> чтобы закрыть существенно больше 0.291 и приблизиться к одиночному оракулу
> 0.701, не выполняя заранее плотный fine-проход?

Протокол зафиксирован: левый паддинг и офсет **по задаче** из
`scripts/eval_libero_bar.sh` (27 задач на 4, 13 на 3). Единый офсет — только
абляция. Прежние замеры шли на едином офсете 3 — см. `FINDINGS.md` §7е.

До ответа полноценную flow-модель не обучать.

---

## Порядок исполнения и ворота

| # | фаза | скрипт | ворота |
|---|---|---|---|
| 0 | привести документацию в порядок | — | выполнено |
| 1 | **K-4a4** прямой тест субмодулярности | `k4a4_submodularity_audit.py` | **выполнено:** глобальная субмодулярность эмпирически опровергнута, классической гарантии жадного отбора нет. Ворот не имеет — направление не закрывает |
| 2 | **B0** causal router dataset | `k4b0_build_router_dataset.py`, `k4b0_verify.py` | **выполнено:** 16000 вмешательств, 1000 наблюдений, 500 эпизодов, 40 задач; все проверки пройдены (§7д) |
| 2.5 | **B0.1** фиксация датасета и baseline | `k4b0_extra_baselines.py`, `k4b0_verify.py` | сверка «ровно K» на всех строках; support-декомпозиция; зафиксированы `B_heur` и `B_prior` по validation |
| 3 | **B1** независимый причинный scorer | `k4b1_train_router.py`, `k4b1_eval_router.py` | `R ≥ 0.60` И `R − B_prior ≥ 0.05` И нижняя граница парного ДИ выше нуля, на трёх сидах. Печатаются оба headroom: `ρ_heur = (R−B_heur)/(0.701−B_heur)`, `ρ_prior` аналогично. `ρ_heur < 0.50` — закрыть; 0.50–0.75 — серая зона. С greedy (0.846) сравнивается B2, не B1 |
| 4 | **B2** взаимозаменяемость и set-модели | там же | sequential оправдан лишь при +0.05 над independent И +0.03 над one-shot, нижняя граница ДИ > 0, latency < 10% сэкономленного. Никакой обучаемый < 0.65 → закрыть ветку |
| 5 | **C0-lite** latency action-only refiner, до B1 | `k4c0_sparse_refiner_bench.py` | одно условие вместо трёх: `f · r ≥ 1 − 1/1.15 = 0.130`, где `f` — доля refiner в сквозном времени, `r = 1 − (16 + (T−1)K)/(16T)` — сэкономленная доля стоимости refiner. Накладные router+top-k+gather/scatter входят в замер. Окончательная проверка — измеренное сквозное ускорение ≥1.15× |
| 6 | **C1** плотный action-only flow | — | стабильное улучшение по шагам, кривая quality/NFE, три сида |
| 7 | **D0** перенос на настоящую траекторию | — | K=4: оракул нижняя ДИ ≥0.75, router ≥0.65, support заметно <16. Оракул <0.70 → закрыть |
| 8 | **D** sparse active-set flow | — | — |
| 9 | **E** адаптивный K | — | сравнение при равном среднем token-NFE |
| 10 | **F** LIBERO | — | success не хуже dense на 1–1.5 п.п. при −15% latency, либо +2 п.п. при равной стоимости |

**Прежние ворота фазы 1 «violation mass ≤ 5%» отменены**: метрика непригодна —
сумма по 12720 тройкам, делённая на один скалярный выигрыш, имеет произвольный
масштаб. Заменена долей нарушений среди содержательных троек, порога не
требующей.

**Решение о продолжении B1 опирается на измеренный разрыв**, а не на
субмодулярную теорию: причинный baseline 0.291, независимый оракул 0.701,
жадный 0.846, точный 0.877 — всё на test датасета B0. Разрыв 0.291 → 0.701 и
есть предмет фазы B1; 0.701 → 0.846 — предмет B2.

Планка высокая, и это надо понимать заранее: порог `R ≥ 0.60` требует
восстановить 86% оракульного одиночного ранжирования из признаков, доступных до
плотного прохода. Если он не берётся, вывод «признаков недостаточно» надо
отличать от «полезность непредсказуема» — через абляции признаков.

**Test датасета B0 считается development-test.** Его агрегаты уже многократно
использовались при проектировании метода. До обучения B1 фиксируется новый
непересекающийся по эпизодам holdout, который открывается один раз после выбора
архитектуры, признаков и весов loss.

**Развилка при провале C0-lite — трёхсторонняя, а не двусторонняя.**

1. сквозное ≥1.15× — заявляется реальная экономия времени;
2. ускорения нет, но разреженная модель бьёт плотную **при равной измеренной
   latency или равных FLOPs** — работа переформулируется в «эффективное
   распределение бюджета», ветка продолжается;
3. нет ни ускорения, ни качества при честно выровненной стоимости — ветка
   закрывается, переход к adaptive NFE.

Выравнивать по token-NFE нельзя: §7б показывает, что стоимость не
масштабируется числом позиций, и token-NFE плохо соответствует времени.

Резервная ветка — adaptive NFE / early stopping плотного action-only flow.
Переходить к ней при провале ворот 4, 7 или пункта 3 развилки выше. Начинать с
оракульного minimal-NFE теста, а не сразу с halting head.

Отложенная ветка — adaptive temporal tokenization. Сейчас данных за неё нет:
полезные позиции разбросаны, multi-segment не эквивалентен произвольному
набору.

## Закрытые ветки

- residual suffix projection как самостоятельная новизна;
- sparse refinement внутри существующего BAR;
- фиксированные временные окна;
- multi-segment router как эквивалент произвольного множества;
- экономия за счёт маскирования выходных токенов плотной модели.

---

## Правило причинности для router

**Можно:** observation/VLM context; старые coarse и fine токены; `lg_before`;
энтропия и запас top1−top2 из `lg_before`; предложенная правка `u → v`;
`e(u)`, `e(v)`, `e(v) − e(u)`; текущая кумулятивная латента; временная позиция;
история предыдущих шагов refinement.

**Нельзя:** `lg_after`; JS между `lg_before` и `lg_after`; fine-коды `z_ref`;
`h_ref`; датасетное действие; результат полного пересчёта.

Значение нового coarse-кода `v` использовать можно — в настоящей модели это уже
предложенное исправление.

## Инженерные правила

Каждый эксперимент обязан: фиксировать commit hash, config и seed; сохранять
episode ID для split; складывать сырые результаты в NPZ; содержать синтетический
self-test с известным ответом; использовать кластерный бутстрап по эпизодам;
отделять непрерывную ошибку действия от схвата; явно маркировать оракульные и
deployable признаки; различать измеренный runtime и аналитическую
экстраполяцию; сравнивать методы при равном compute; не менять метрику после
просмотра результата; не обновлять `FINDINGS.md` до проверки кода и повторного
запуска.
