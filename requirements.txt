# Замеры на выпущенных весах ActionCodec. GPU нужен для VLA-чекпойнтов;
# для замеров на одном токенизаторе хватает CPU.
# torch приходит из базового образа.

# ВЕРХНЯЯ ГРАНИЦА ОТ ActionCodec: их requirements.txt требует numpy<2.0,
# на 2.x их код не проверялся.
numpy>=1.24,<2
scipy>=1.11

# ActionCodec, ядро. Версия vector-quantize-pytorch ЖЁСТКАЯ — так у них
# (requirements.txt), API этой библиотеки между минорными версиями менялся.
vector-quantize-pytorch==1.27.15
einops
safetensors>=0.4
termcolor
hydra-core>=1.3
omegaconf>=2.3

# ВЕРХНЯЯ ГРАНИЦА ОБЯЗАТЕЛЬНА: hub 1.x ломает transformers и tokenizers.
# Скачивать питоновским API hf_hub_download, а не CLI — тот ставится
# в ~/.local/bin вне PATH.
huggingface_hub>=0.34,<1.0
hf_transfer>=0.1
# SmolVLMConfig появился в transformers 4.50; прежняя граница <4.50 была
# унаследована из другого проекта (prismatic/VQ-VLA), здесь его нет.
# ВЕРХНЯЯ ГРАНИЦА ОБЯЗАТЕЛЬНА: transformers 5.x меняет API и тянет
# huggingface-hub 1.x с tokenizers 0.22.
transformers>=4.50,<4.57
tokenizers<0.22
accelerate>=0.30,<2
num2words>=0.5

# torch и его спутники ПРИБИТЫ к версии базового образа. Установка любого из
# них без пина утащит остальные: `pip install -U torchvision` однажды подняла
# torch 2.4.1 -> 2.13.0 и поставила пакеты nvidia-cu13 поверх сборки под
# CUDA 12.4. Ставить только с --index-url на нужный CUDA-канал.
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# датасеты LIBERO
zarr>=2.16,<3
numcodecs>=0.12,<0.16
h5py>=3.10
pyarrow>=15

matplotlib>=3.8
tqdm>=4.66

# --- Ниже НЕ нужно для замеров на токенизаторе; ставить, когда дойдём до
# --- роллаутов в LIBERO. Версии взяты из ActionCodec/requirements.txt.
# torchvision
# imageio
# lightning
# peft
# mujoco==3.2.3
# robosuite==1.4.1
# libero==0.1.0
# libero2gym==0.1.0
# gym==0.22.0
